<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_inherit_sync" model="ir.ui.view">
        <name>res.partner.form.inherit.sync</name>
        <model>res.partner</model>
        <inherit_id ref="base.view_partner_form"/>
        <arch type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_sync_to_odoo2" 
                        string="Tạo/Sync Contact Odoo 2" 
                        type="object" 
                        class="btn-primary"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Trạng thái Sync Odoo 2" name="sync_odoo2_page">
                    <group>
                        <group string="Thông tin đồng bộ">
                            <field name="sync_status" widget="badge" 
                                   decoration-success="sync_status == 'success'" 
                                   decoration-danger="sync_status == 'failed'" 
                                   decoration-muted="sync_status == 'draft'"/>
                            <field name="remote_partner_id" readonly="1"/>
                            <field name="retry_count" readonly="1"/>
                        </group>
                        <group string="Chi tiết lỗi">
                            <field name="sync_error_msg" readonly="1"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </arch>
    </record>
</odoo>
