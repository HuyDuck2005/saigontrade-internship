=== CẤU TRÚC THƯ MỤC ===
.
./Lead_Website_Odoo_Solution.md
./Lead_Website_Odoo_Solution.md:Zone.Identifier
./__init__.py
./__manifest__.py
./controllers
./controllers/__init__.py
./controllers/home_override.py
./controllers/main.py
./data
./data/lead_category_data.xml
./data/test_lead_data.xml
./models
./models/__init__.py
./models/approval_history.py
./models/crm_lead_extension.py
./models/crm_lead_gallery.py
./models/crm_lead_image.py
./models/lead_category.py
./project_code_dump.txt
./security
./security/ir.model.access.csv
./security/ir_model_access.xml
./security/security_gallery.xml
./security/security_image.xml
./static
./static/css
./static/css/lead_cards.css
./static/js
./static/js/lead_portal.js
./views
./views/crm_lead_gallery_views.xml
./views/crm_lead_views.xml
./views/dashboard_views.xml
./views/email_templates.xml
./views/lead_reject_wizard_views.xml
./views/website_lead_gallery_template.xml
./views/website_templates.xml
./wizard
./wizard/__init__.py
./wizard/lead_reject_wizard.py

========================================

=== NỘI DUNG CÁC FILE ===

----------------------------------------
FILE: ./Lead_Website_Odoo_Solution.md
----------------------------------------

# Phương Án Thiết Kế Hệ Thống Lead Website trong Odoo

## 📋 Mục Lục
1. [Tổng Quan Kiến Trúc](#tổng-quan-kiến-trúc)
2. [Modules Cần Thiết](#modules-cần-thiết)
3. [Models và Data Structure](#models-và-data-structure)
4. [Luồng Dữ Liệu](#luồng-dữ-liệu)
5. [Luồng Nghiệp Vụ Chi Tiết](#luồng-nghiệp-vụ-chi-tiết)
6. [Hướng Dẫn Triển Khai](#hướng-dẫn-triển-khai)
7. [Các Tính Năng Có Thể/Không Thể Thực Hiện](#các-tính-năng-có-thểkhông-thể-thực-hiện)
8. [Bổ Sung và Tối Ưu](#bổ-sung-và-tối-ưu)

---

## 🏗️ Tổng Quan Kiến Trúc

### Kiến Trúc Tổng Thể

```
┌─────────────────────────────────────────────────────────────┐
│                      CRM (Odoo Backend)                      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ Lead Model (crm.lead)                                │   │
│  │ - Lead Name                                          │   │
│  │ - Lead Image                                         │   │
│  │ - Category / Nhu cầu                                 │   │
│  │ - Country / Region                                   │   │
│  │ - Description                                        │   │
│  │ - Contact Info                                       │   │
│  │ - Created Date                                       │   │
│  │ - Approval Status (draft→approved)                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│               Admin Approval Workflow                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ Chức năng: Review → Approve/Reject Leads             │   │
│  │ Dashboard: Leads cần duyệt                           │   │
│  │ Notification: Gửi thông báo đến Admin                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│              Website Portal (Public)                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ Lead Card List View                                  │   │
│  │ - Grid/List Layout                                   │   │
│  │ - Filter by Category/Country                         │   │
│  │ - Search Functionality                               │   │
│  │ - Pagination                                         │   │
│  │                                                      │   │
│  │ Lead Detail View                                     │   │
│  │ - Full Lead Information                              │   │
│  │ - Contact Form                                       │   │
│  │ - Related Leads                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Modules Cần Thiết

### Modules Odoo Chuẩn (Có sẵn)
- **crm** - Quản lý Lead
- **website** - Website engine
- **website_crm** - Integration CRM-Website (hoặc tạo tùy chỉnh)
- **email_template** - Gửi email thông báo
- **web** - Backend UI

### Module Tùy Chỉnh Cần Tạo
- **website_lead_showcase** - Module chính cho hệ thống Lead Website

### Cấu trúc Module

```
website_lead_showcase/
├── __init__.py
├── __manifest__.py
├── models/
│   ├── __init__.py
│   ├── crm_lead_extension.py      # Mở rộng crm.lead model
│   └── website_lead_portal.py      # Website-specific lead data
├── controllers/
│   ├── __init__.py
│   └── main.py                     # Website controllers
├── views/
│   ├── crm_lead_views.xml          # Backend views
│   ├── website_templates.xml        # Frontend templates
│   └── email_templates.xml          # Email templates
├── static/
│   ├── css/
│   │   └── lead_cards.css
│   ├── js/
│   │   └── lead_portal.js
│   └── images/
│       └── placeholders/
├── security/
│   └── ir_model_access.xml         # Access control
├── data/
│   └── lead_category_data.xml      # Default categories
└── report/
    └── lead_portal_report.xml      # Reports
```

---

## 🗂️ Models và Data Structure

### 1. CRM Lead Model (Mở rộng)

#### Fields Cần Thêm/Sửa

```python
# models/crm_lead_extension.py

class CrmLead(models.Model):
    _inherit = 'crm.lead'
    
    # ===== FIELDS HIỂN THỊ WEBSITE =====
    website_visible = fields.Boolean(
        string="Hiển thị trên Website",
        default=False,
        help="Có hiển thị lead này trên website công khai hay không"
    )
    
    approval_status = fields.Selection([
        ('draft', 'Chưa duyệt'),
        ('approved', 'Đã duyệt'),
        ('rejected', 'Từ chối'),
    ], string="Trạng thái duyệt", default='draft')
    
    approval_date = fields.Datetime(
        string="Ngày duyệt",
        readonly=True,
        help="Thời gian Admin phê duyệt lead"
    )
    
    approved_by = fields.Many2one(
        'res.users',
        string="Người duyệt",
        readonly=True
    )
    
    rejection_reason = fields.Text(
        string="Lý do từ chối",
        help="Lý do nếu admin từ chối duyệt lead"
    )
    
    # ===== FIELDS HIỂN THỊ TRÊN CARD =====
    lead_image = fields.Image(
        string="Hình ảnh Lead",
        help="Ảnh đại diện cho lead trên website"
    )
    
    lead_category = fields.Many2one(
        'lead.category',
        string="Loại nhu cầu/Lĩnh vực",
        help="Phân loại nhu cầu của lead"
    )
    
    country_region = fields.Char(
        string="Quốc gia/Khu vực",
        help="Nơi lead đó cần hỗ trợ"
    )
    
    short_description = fields.Text(
        string="Mô tả ngắn",
        help="Mô tả tóm tắt về lead (hiển thị trên card)"
    )
    
    website_published_date = fields.Datetime(
        string="Ngày xuất bản",
        readonly=True,
        help="Thời gian lead được xuất bản trên website"
    )
    
    contact_person = fields.Char(
        string="Người liên hệ",
        help="Tên người liên hệ"
    )
    
    contact_phone = fields.Char(
        string="Số điện thoại liên hệ",
        help="Điện thoại để liên hệ"
    )
    
    contact_email = fields.Char(
        string="Email liên hệ",
        help="Email để liên hệ"
    )
    
    # ===== FIELDS THỐNG KỊ =====
    website_view_count = fields.Integer(
        string="Lượt xem trên website",
        default=0,
        readonly=True
    )
    
    website_contact_count = fields.Integer(
        string="Số lần được liên hệ từ website",
        default=0,
        readonly=True
    )
    
    # ===== WORKFLOW FIELDS =====
    last_approval_action = fields.Selection([
        ('submitted', 'Vừa được submit'),
        ('reviewed', 'Đã được review'),
        ('approved', 'Đã duyệt'),
        ('rejected', 'Bị từ chối'),
        ('resubmitted', 'Được resubmit sau từ chối'),
    ], string="Hành động duyệt cuối cùng", readonly=True)
    
    approval_history = fields.One2many(
        'lead.approval.history',
        'lead_id',
        string="Lịch sử duyệt",
        readonly=True
    )
```

#### Lead Category Model

```python
# models/lead_category.py

class LeadCategory(models.Model):
    _name = 'lead.category'
    _description = 'Loại nhu cầu / Lĩnh vực Lead'
    
    name = fields.Char(string="Tên loại", required=True)
    code = fields.Char(string="Mã loại", required=True, unique=True)
    description = fields.Text(string="Mô tả")
    icon = fields.Char(string="Icon", help="Font Awesome icon class")
    color = fields.Char(string="Màu sắc", help="Hex color code")
    sequence = fields.Integer(string="Thứ tự", default=10)
    active = fields.Boolean(default=True)
    
    lead_ids = fields.One2many('crm.lead', 'lead_category', string="Leads")
    lead_count = fields.Integer(
        string="Số Lead",
        compute='_compute_lead_count'
    )
    
    def _compute_lead_count(self):
        for category in self:
            category.lead_count = len(category.lead_ids.filtered(
                lambda l: l.approval_status == 'approved'
            ))
```

#### Approval History Model

```python
# models/approval_history.py

class LeadApprovalHistory(models.Model):
    _name = 'lead.approval.history'
    _description = 'Lịch sử duyệt Lead'
    _order = 'create_date desc'
    
    lead_id = fields.Many2one('crm.lead', string="Lead", required=True)
    action = fields.Selection([
        ('submitted', 'Submit'),
        ('approved', 'Phê duyệt'),
        ('rejected', 'Từ chối'),
        ('resubmitted', 'Gửi lại'),
    ], string="Hành động", required=True)
    
    performed_by = fields.Many2one('res.users', string="Người thực hiện")
    notes = fields.Text(string="Ghi chú")
    create_date = fields.Datetime(string="Thời gian", readonly=True)
    
    def _default_performed_by(self):
        return self.env.user.id
```

---

## 🔄 Luồng Dữ Liệu

### Luồng 1: Tạo Lead → Duyệt → Xuất bản

```
1. Sales/Marketer tạo Lead trên CRM Backend
   └─ approval_status = 'draft'
   └─ website_visible = False
   └─ Tự động ghi record lịch sử (submitted)

2. Gửi Email Thông báo đến Admin
   ├─ Email subject: "Cần duyệt Lead: [Lead Name]"
   ├─ Email body: 
   │  └─ Lead details
   │  └─ Link đến trang review
   │  └─ Nút "Phê duyệt" / "Từ chối"
   └─ Recipient: Group "Lead Approvers" hoặc specific admin user

3. Admin Review Dashboard
   ├─ Xem danh sách Lead cần duyệt (approval_status = 'draft')
   ├─ Click vào Lead để xem chi tiết
   └─ Chọn: [Phê duyệt] hoặc [Từ chối]
   
   3a. Nếu chọn [Phê duyệt]:
       ├─ approval_status = 'approved'
       ├─ website_visible = True
       ├─ website_published_date = now()
       ├─ approved_by = current_user
       ├─ approval_date = now()
       ├─ Ghi lịch sử (approved)
       ├─ Gửi email thông báo "Lead đã được duyệt" đến requester
       └─ Lead xuất hiện trên Website
   
   3b. Nếu chọn [Từ chối]:
       ├─ approval_status = 'rejected'
       ├─ website_visible = False
       ├─ rejection_reason = [Lý do Admin nhập]
       ├─ Ghi lịch sử (rejected)
       └─ Gửi email thông báo "Lead bị từ chối" kèm lý do

4. Website Portal
   ├─ Truy vấn: Lead.search([
   │  ('approval_status', '=', 'approved'),
   │  ('website_visible', '=', True)
   │ ])
   └─ Hiển thị dưới dạng Cards/Grid
```

### Luồng 2: Quản lý trên Website

```
1. Khách hàng truy cập Website
   ├─ Xem trang danh sách Lead (Lead Portal)
   └─ Các Lead có approval_status = 'approved' được hiển thị

2. Tương tác trên danh sách:
   ├─ Filter by Category
   ├─ Filter by Country/Region
   ├─ Search by keyword
   ├─ Sort by Date/View Count
   └─ Pagination

3. Click vào Card → Lead Detail Page
   ├─ Hiển thị đầy đủ thông tin Lead
   ├─ website_view_count += 1
   ├─ Hiển thị Contact Form (để người dùng liên hệ)
   ├─ Show Related Leads (cùng category)
   └─ CTA: "Liên hệ ngay" / "Xem chi tiết"

4. Khách hàng submit Contact Form
   ├─ Tạo Lead mới hoặc update Opportunity
   ├─ website_contact_count += 1
   ├─ Gửi email notification đến sales team
   ├─ Gửi email confirmation đến khách hàng
   └─ Tạo activity/note trên Lead gốc
```

---

## 📊 Luồng Nghiệp Vụ Chi Tiết

### STATE MACHINE: Lead Lifecycle

```
┌─────────┐
│  DRAFT  │  [Initial Status]
└────┬────┘
     │ [Action: Submit]
     ▼
┌──────────────────┐
│ PENDING APPROVAL │  [Waiting for Admin Review]
└────┬────┬────────┘
     │    │
  [Approve] [Reject]
     │    │
     ▼    ▼
┌──────┐ ┌──────────┐
│ACTIVE│ │ REJECTED │
└──────┘ └─────┬────┘
              │ [Can Resubmit]
              ▼
         ┌──────────────┐
         │ RESUBMITTED  │
         └──────────────┘
              │
           [Review Again]
              ▼
         ┌──────┐ ┌──────────┐
         │ACTIVE│ │ REJECTED │
         └──────┘ └──────────┘
```

### BUSINESS RULES & CONDITIONS

#### Rule 1: Điều kiện để Lead xuất hiện trên Website

**Điều kiện Cần (Necessary):**
- `approval_status == 'approved'`
- `website_visible == True`
- Lead không bị lưu trữ/xóa

**Điều kiện Đủ (Sufficient):**
- Tất cả điều kiện cần được thỏa mãn
- `website_published_date` không null
- Lead có hình ảnh (lead_image not null)
- Lead có mô tả (short_description not null)

**Công thức Logic:**
```
Lead Visible = (approval_status == 'approved') 
             AND (website_visible == True) 
             AND (active == True)
             AND (lead_image IS NOT NULL)
             AND (short_description IS NOT NULL)
```

#### Rule 2: Quyền Phê Duyệt

**Có thể phê duyệt nếu:**
- User thuộc group "Lead Approvers"
- Lead có approval_status == 'draft' hoặc 'rejected'
- User hiện tại != người tạo Lead (avoid conflict)

**Không thể phê duyệt nếu:**
- User không có quyền
- Lead đã ở trạng thái 'approved' (không thể approve lại)
- Lead có missing mandatory fields

#### Rule 3: Thông Báo và Workflow

**Khi Lead được tạo:**
- Tự động ghi lịch sử: `(submitted, created_by_user)`
- Gửi email đến "Lead Approvers" group
- Tạo activity task cho Admin

**Khi Lead được Phê Duyệt:**
- Ghi lịch sử: `(approved, approved_by_user, timestamp)`
- Gửi email xác nhận đến người tạo
- Publish lead đến website (update published_date)
- Tạo activity: "Lead approved - now visible on website"

**Khi Lead bị Từ Chối:**
- Ghi lịch sử: `(rejected, approved_by_user, timestamp, reason)`
- Gửi email từ chối kèm lý do đến người tạo
- Không xuất hiện trên website

**Khi Lead được Gửi Lại (Resubmit):**
- Chuyển approval_status → 'draft'
- Ghi lịch sử: `(resubmitted, submitted_by_user, timestamp)`
- Gửi email thông báo đến approver lần 2

#### Rule 4: Quản Lý Thời Gian

| Sự kiện | Trường Lưu | Loại | Notes |
|---------|-----------|------|-------|
| Lead được tạo | created_date (Odoo) | Datetime | Auto-set by system |
| Lead được submit | `N/A` | - | Ghi trong approval_history |
| Lead được duyệt | approval_date | Datetime | Manual set by approver |
| Lead được xuất bản | website_published_date | Datetime | Auto-set khi approved |
| Lead được từ chối | `N/A` | - | Ghi trong approval_history |

#### Rule 5: Dữ Liệu Hiển Thị Trên Card

**Bắt buộc phải có:**
1. lead_image (Hình ảnh)
2. name (Tiêu đề Lead)
3. short_description (Mô tả ngắn)

**Nên có:**
4. lead_category (Loại nhu cầu)
5. country_region (Quốc gia)
6. website_published_date (Ngày đăng)

**Có thể có:**
7. contact_person / contact_phone / contact_email (Thông tin liên hệ)

---

## 🚀 Hướng Dẫn Triển Khai

### Phase 1: Preparation (Tuần 1)

#### 1.1 Chuẩn Bị Odoo Environment

```bash
# Tạo backup database trước
# Chuyển đến thư mục addons
cd /path/to/odoo/addons

# Clone hoặc tạo module mới
mkdir website_lead_showcase
cd website_lead_showcase
```

#### 1.2 Tạo Module Manifest

```python
# __manifest__.py
{
    'name': 'Website Lead Showcase',
    'version': '15.0.1.0.0',
    'category': 'Website',
    'sequence': 1,
    'author': 'Your Company',
    'depends': ['website', 'crm', 'website_crm', 'sale_crm'],
    'installable': True,
    'application': False,
    'data': [
        # Security
        'security/ir_model_access.xml',
        
        # Data
        'data/lead_category_data.xml',
        
        # Views - Backend
        'views/crm_lead_views.xml',
        'views/lead_approval_views.xml',
        'views/dashboard_views.xml',
        
        # Views - Frontend
        'views/website_templates.xml',
        'views/email_templates.xml',
    ],
    'assets': {
        'web.assets_backend': [
            'website_lead_showcase/static/css/backend.css',
        ],
        'web.assets_frontend': [
            'website_lead_showcase/static/css/lead_cards.css',
            'website_lead_showcase/static/js/lead_portal.js',
        ],
    },
    'external_dependencies': {
        'python': [],
        'bin': [],
    },
}
```

#### 1.3 Tạo Module Init Files

```python
# __init__.py
from . import models, controllers
```

```python
# models/__init__.py
from . import crm_lead_extension
from . import lead_category
from . import lead_approval_history
```

```python
# controllers/__init__.py
from . import main
```

### Phase 2: Backend Development (Tuần 1-2)

#### 2.1 Implement Models

```python
# models/crm_lead_extension.py

from odoo import models, fields, api
from odoo.exceptions import ValidationError

class CrmLead(models.Model):
    _inherit = 'crm.lead'
    
    # ... [Fields định nghĩa ở trên]
    
    @api.model
    def create(self, vals):
        """Override create để tự động ghi approval history"""
        record = super().create(vals)
        
        # Tạo approval history record
        self.env['lead.approval.history'].create({
            'lead_id': record.id,
            'action': 'submitted',
            'performed_by': self.env.user.id,
            'notes': f"Lead được tạo bởi {self.env.user.name}",
        })
        
        # Gửi email thông báo đến approvers
        self._send_approval_notification(record)
        
        return record
    
    def action_approve(self):
        """Phê duyệt Lead"""
        # Check permission
        if not self.env.user.has_group('website_lead_showcase.group_lead_approver'):
            raise ValidationError("Bạn không có quyền phê duyệt Lead")
        
        # Check business rules
        if self.approval_status == 'approved':
            raise ValidationError("Lead này đã được phê duyệt rồi")
        
        if not self.lead_image:
            raise ValidationError("Lead phải có hình ảnh trước khi phê duyệt")
        
        if not self.short_description:
            raise ValidationError("Lead phải có mô tả trước khi phê duyệt")
        
        # Update lead
        self.write({
            'approval_status': 'approved',
            'website_visible': True,
            'website_published_date': fields.Datetime.now(),
            'approved_by': self.env.user.id,
            'approval_date': fields.Datetime.now(),
        })
        
        # Record history
        self.env['lead.approval.history'].create({
            'lead_id': self.id,
            'action': 'approved',
            'performed_by': self.env.user.id,
            'notes': 'Lead được phê duyệt và xuất bản trên website',
        })
        
        # Send confirmation email
        self._send_approval_confirmation_email()
    
    def action_reject(self):
        """Từ chối Lead"""
        return {
            'type': 'ir.actions.act_window',
            'res_model': 'lead.reject.wizard',
            'view_mode': 'form',
            'target': 'new',
            'context': {'default_lead_id': self.id},
        }
    
    def action_resubmit(self):
        """Gửi lại Lead"""
        if self.approval_status != 'rejected':
            raise ValidationError("Chỉ có thể gửi lại Lead bị từ chối")
        
        self.write({
            'approval_status': 'draft',
            'rejection_reason': '',
        })
        
        self.env['lead.approval.history'].create({
            'lead_id': self.id,
            'action': 'resubmitted',
            'performed_by': self.env.user.id,
        })
        
        self._send_approval_notification(self)
    
    def _send_approval_notification(self, record):
        """Gửi email thông báo đến approvers"""
        template = self.env.ref(
            'website_lead_showcase.email_lead_approval_request'
        )
        
        approvers = self.env['res.users'].search([
            ('groups_id', 'in', 
             self.env.ref('website_lead_showcase.group_lead_approver').id)
        ])
        
        for approver in approvers:
            template.send_mail(record.id, force_send=True)
    
    def _send_approval_confirmation_email(self):
        """Gửi email xác nhận phê duyệt"""
        template = self.env.ref(
            'website_lead_showcase.email_lead_approved'
        )
        template.send_mail(self.id, force_send=True)
    
    @api.constrains('lead_image', 'short_description')
    def _check_required_fields_for_approval(self):
        """Kiểm tra fields bắt buộc khi phê duyệt"""
        for record in self:
            if record.approval_status == 'approved':
                if not record.lead_image:
                    raise ValidationError(
                        f"Lead '{record.name}' phải có hình ảnh"
                    )
                if not record.short_description:
                    raise ValidationError(
                        f"Lead '{record.name}' phải có mô tả"
                    )
```

#### 2.2 Views - CRM Lead Backend

```xml
<!-- views/crm_lead_views.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="crm_lead_form_inherit_website" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.website</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_form_view"/>
        <field name="arch" type="xml">
            
            <!-- Thêm tab Website Info -->
            <xpath expr="//notebook" position="inside">
                <page string="Website &amp; Approval" name="website_info">
                    
                    <!-- Status Section -->
                    <group string="Approval Status">
                        <field name="approval_status" 
                               widget="statusbar"
                               attrs="{'readonly': [('approval_status', '=', 'approved')]}"/>
                        <field name="website_visible"/>
                        <field name="approved_by" readonly="1"/>
                        <field name="approval_date" readonly="1"/>
                    </group>
                    
                    <!-- Website Display Info -->
                    <group string="Thông tin hiển thị Website">
                        <field name="lead_image" widget="image"/>
                        <field name="lead_category"/>
                        <field name="country_region"/>
                        <field name="website_published_date" readonly="1"/>
                    </group>
                    
                    <!-- Mô tả -->
                    <group string="Mô tả">
                        <field name="short_description" nolabel="1"/>
                    </group>
                    
                    <!-- Contact Info -->
                    <group string="Thông tin liên hệ">
                        <field name="contact_person"/>
                        <field name="contact_phone"/>
                        <field name="contact_email"/>
                    </group>
                    
                    <!-- Statistics -->
                    <group string="Thống kê Website">
                        <field name="website_view_count" readonly="1"/>
                        <field name="website_contact_count" readonly="1"/>
                    </group>
                    
                    <!-- Approval History -->
                    <field name="approval_history" readonly="1" nolabel="1">
                        <tree create="0" delete="0">
                            <field name="create_date"/>
                            <field name="action"/>
                            <field name="performed_by"/>
                            <field name="notes"/>
                        </tree>
                    </field>
                    
                    <!-- Actions -->
                    <div class="oe_button_box">
                        <button name="action_approve" 
                                type="object" 
                                class="oe_highlight" 
                                string="Phê duyệt"
                                attrs="{'invisible': [('approval_status', '=', 'approved')]}"/>
                        <button name="action_reject" 
                                type="object" 
                                string="Từ chối"
                                attrs="{'invisible': [('approval_status', 'in', ['approved', 'rejected'])]}"/>
                        <button name="action_resubmit" 
                                type="object" 
                                string="Gửi lại"
                                attrs="{'invisible': [('approval_status', '!=', 'rejected')]}"/>
                    </div>
                    
                    <!-- Rejection Reason -->
                    <group string="Lý do từ chối" attrs="{'invisible': [('approval_status', '!=', 'rejected')]}">
                        <field name="rejection_reason" nolabel="1"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
    
    <!-- List View cho quản lý Lead -->
    <record id="crm_lead_tree_view_website" model="ir.ui.view">
        <field name="name">crm.lead.tree.website</field>
        <field name="model">crm.lead</field>
        <field name="type">tree</field>
        <field name="arch" type="xml">
            <tree string="Leads - Website Management"
                  decoration-success="approval_status=='approved'"
                  decoration-warning="approval_status=='draft'"
                  decoration-danger="approval_status=='rejected'">
                <field name="name"/>
                <field name="lead_category"/>
                <field name="approval_status" widget="badge"/>
                <field name="website_visible"/>
                <field name="approval_date"/>
                <field name="website_view_count"/>
            </tree>
        </field>
    </record>
    
    <!-- Search View -->
    <record id="crm_lead_search_view_website" model="ir.ui.view">
        <field name="name">crm.lead.search.website</field>
        <field name="model">crm.lead</field>
        <field name="type">search</field>
        <field name="arch" type="xml">
            <search string="Leads">
                <field name="name" string="Lead Name"/>
                <field name="lead_category"/>
                <field name="approval_status"/>
                <separator/>
                <filter name="approved" 
                        string="Approved" 
                        domain="[('approval_status', '=', 'approved')]"/>
                <filter name="draft" 
                        string="Draft" 
                        domain="[('approval_status', '=', 'draft')]"/>
                <filter name="rejected" 
                        string="Rejected" 
                        domain="[('approval_status', '=', 'rejected')]"/>
                <separator/>
                <filter name="website_visible" 
                        string="Visible on Website" 
                        domain="[('website_visible', '=', True)]"/>
            </search>
        </field>
    </record>
    
</odoo>
```

#### 2.3 Admin Dashboard

```xml
<!-- views/dashboard_views.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Approval Dashboard -->
    <record id="lead_approval_dashboard" model="ir.ui.view">
        <field name="name">Lead Approval Dashboard</field>
        <field name="model">crm.lead</field>
        <field name="type">kanban</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="id"/>
                <field name="name"/>
                <field name="approval_status"/>
                <field name="lead_image"/>
                <field name="short_description"/>
                
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <div class="o_kanban_image">
                                <img t-if="record.lead_image.raw_value" 
                                     t-att-src="kanban_image('crm.lead', 'lead_image', record.id.raw_value)" 
                                     alt="Lead Image"/>
                            </div>
                            <div class="oe_kanban_details">
                                <strong><field name="name"/></strong>
                                <div>
                                    <field name="approval_status" widget="badge"/>
                                </div>
                                <div class="text-muted">
                                    <field name="short_description" 
                                           widget="text" 
                                           class="text-truncate"/>
                                </div>
                                <div class="oe_kanban_footer_left">
                                    <button name="action_approve" 
                                            type="object" 
                                            class="btn btn-sm btn-success"
                                            attrs="{'invisible': [('approval_status', '=', 'approved')]}"
                                            string="✓ Approve"/>
                                    <button name="action_reject" 
                                            type="object" 
                                            class="btn btn-sm btn-danger"
                                            string="✗ Reject"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    
    <!-- Menu Items -->
    <menuitem id="menu_lead_management" 
              name="Lead Management" 
              parent="crm.crm_menu_root" 
              sequence="20"/>
    
    <menuitem id="menu_lead_approval" 
              name="Approval Queue" 
              parent="menu_lead_management" 
              action="action_crm_lead_approval_queue"/>
    
    <menuitem id="menu_lead_categories" 
              name="Lead Categories" 
              parent="menu_lead_management" 
              action="action_lead_category_list"/>
    
</odoo>
```

### Phase 3: Frontend Development (Tuần 2-3)

#### 3.1 Website Controllers

```python
# controllers/main.py

from odoo import http, fields
from odoo.http import request

class LeadPortalController(http.Controller):
    
    @http.route('/leads', auth='public', website=True, sitemap=True)
    def leads_list(self, **kwargs):
        """Trang danh sách Lead"""
        
        # Lấy filter parameters
        category_id = kwargs.get('category_id')
        country = kwargs.get('country')
        search_query = kwargs.get('search')
        page = int(kwargs.get('page', 1))
        per_page = 12
        
        # Build domain
        domain = [
            ('approval_status', '=', 'approved'),
            ('website_visible', '=', True),
        ]
        
        if category_id:
            domain.append(('lead_category', '=', int(category_id)))
        
        if country:
            domain.append(('country_region', 'ilike', country))
        
        if search_query:
            domain.append(('name', 'ilike', search_query))
        
        # Lấy leads
        leads = request.env['crm.lead'].search(
            domain, 
            order='website_published_date desc'
        )
        
        # Pagination
        total = len(leads)
        offset = (page - 1) * per_page
        leads = leads[offset:offset + per_page]
        
        # Lấy categories cho filter
        categories = request.env['lead.category'].search([])
        
        # Lấy danh sách countries
        countries = request.env['crm.lead'].search(
            [('approval_status', '=', 'approved')]
        ).mapped('country_region')
        countries = list(set([c for c in countries if c]))
        
        return request.render('website_lead_showcase.leads_list', {
            'leads': leads,
            'categories': categories,
            'countries': sorted(countries),
            'total': total,
            'page': page,
            'per_page': per_page,
            'total_pages': (total + per_page - 1) // per_page,
            'search_query': search_query,
            'selected_category': category_id,
            'selected_country': country,
        })
    
    @http.route('/leads/<int:lead_id>', auth='public', website=True)
    def lead_detail(self, lead_id, **kwargs):
        """Trang chi tiết Lead"""
        
        lead = request.env['crm.lead'].browse(lead_id)
        
        # Check permission
        if lead.approval_status != 'approved' or not lead.website_visible:
            return request.redirect('/leads')
        
        # Increment view count
        lead.website_view_count += 1
        
        # Get related leads
        related_leads = request.env['crm.lead'].search([
            ('approval_status', '=', 'approved'),
            ('website_visible', '=', True),
            ('lead_category', '=', lead.lead_category.id),
            ('id', '!=', lead_id),
        ], limit=4)
        
        return request.render('website_lead_showcase.lead_detail', {
            'lead': lead,
            'related_leads': related_leads,
        })
    
    @http.route('/leads/<int:lead_id>/contact', type='json', auth='public')
    def lead_contact(self, lead_id, **kwargs):
        """Submit contact form"""
        
        lead = request.env['crm.lead'].browse(lead_id)
        
        if lead.approval_status != 'approved':
            return {'status': 'error', 'message': 'Lead not found'}
        
        # Extract form data
        contact_name = kwargs.get('contact_name')
        contact_email = kwargs.get('contact_email')
        contact_phone = kwargs.get('contact_phone')
        message = kwargs.get('message')
        
        # Validate
        if not all([contact_name, contact_email, message]):
            return {'status': 'error', 'message': 'Missing required fields'}
        
        # Create new lead/opportunity
        new_lead = request.env['crm.lead'].create({
            'name': f"Inquiry from {contact_name} - {lead.name}",
            'email_from': contact_email,
            'phone': contact_phone,
            'description': message,
            'parent_id': lead.id,
            'type': 'opportunity',
        })
        
        # Increment contact count
        lead.website_contact_count += 1
        
        # Send notification email
        template = request.env.ref(
            'website_lead_showcase.email_lead_inquiry'
        )
        template.send_mail(new_lead.id, force_send=True)
        
        return {
            'status': 'success',
            'message': 'Thank you! We will contact you soon.'
        }
    
    @http.route('/api/leads/stats', type='json', auth='public')
    def get_stats(self):
        """API để lấy thống kê"""
        
        leads = request.env['crm.lead'].search([
            ('approval_status', '=', 'approved'),
            ('website_visible', '=', True),
        ])
        
        categories = request.env['lead.category'].search([])
        
        stats = {
            'total_leads': len(leads),
            'total_categories': len(categories),
            'categories_breakdown': [
                {
                    'name': cat.name,
                    'count': len(leads.filtered(
                        lambda l: l.lead_category.id == cat.id
                    ))
                }
                for cat in categories
            ]
        }
        
        return stats
```

#### 3.2 Website Templates

```xml
<!-- views/website_templates.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Leads List Template -->
    <template id="leads_list" name="Leads List">
        <t t-call="website.layout">
            <div class="container my-5">
                
                <!-- Header -->
                <div class="row mb-5">
                    <div class="col-lg-8">
                        <h1 class="display-4">Business Opportunities</h1>
                        <p class="lead">Discover the latest leads and opportunities</p>
                    </div>
                </div>
                
                <!-- Filters -->
                <div class="row mb-4">
                    <div class="col-lg-12">
                        <form method="get" class="row g-3">
                            
                            <!-- Search -->
                            <div class="col-md-4">
                                <input type="text" 
                                       name="search" 
                                       class="form-control" 
                                       placeholder="Search leads..."
                                       t-att-value="search_query"/>
                            </div>
                            
                            <!-- Category Filter -->
                            <div class="col-md-3">
                                <select name="category_id" class="form-select">
                                    <option value="">All Categories</option>
                                    <t t-foreach="categories" t-as="cat">
                                        <option t-att-value="cat.id"
                                                t-att-selected="str(cat.id) == str(selected_category)">
                                            <t t-esc="cat.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            
                            <!-- Country Filter -->
                            <div class="col-md-3">
                                <select name="country" class="form-select">
                                    <option value="">All Countries</option>
                                    <t t-foreach="countries" t-as="country">
                                        <option t-att-value="country"
                                                t-att-selected="country == selected_country">
                                            <t t-esc="country"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            
                            <!-- Search Button -->
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-primary w-100">
                                    Search
                                </button>
                            </div>
                            
                        </form>
                    </div>
                </div>
                
                <!-- Leads Grid -->
                <div class="row">
                    <t t-if="leads">
                        <t t-foreach="leads" t-as="lead">
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="card h-100 lead-card shadow-sm">
                                    
                                    <!-- Lead Image -->
                                    <div class="lead-image-container">
                                        <t t-if="lead.lead_image">
                                            <img t-attf-src="data:image/png;base64,{lead.lead_image}" 
                                                 class="card-img-top" 
                                                 alt="Lead"/>
                                        </t>
                                        <t t-else="">
                                            <div class="card-img-top bg-light d-flex align-items-center justify-content-center"
                                                 style="height: 250px;">
                                                <i class="fa fa-image fa-3x text-muted"></i>
                                            </div>
                                        </t>
                                    </div>
                                    
                                    <!-- Card Body -->
                                    <div class="card-body">
                                        
                                        <!-- Category Badge -->
                                        <t t-if="lead.lead_category">
                                            <span class="badge bg-secondary mb-2">
                                                <t t-esc="lead.lead_category.name"/>
                                            </span>
                                        </t>
                                        
                                        <!-- Lead Title -->
                                        <h5 class="card-title">
                                            <t t-esc="lead.name"/>
                                        </h5>
                                        
                                        <!-- Country/Region -->
                                        <p class="text-muted small">
                                            <i class="fa fa-map-marker"></i>
                                            <t t-esc="lead.country_region"/>
                                        </p>
                                        
                                        <!-- Description -->
                                        <p class="card-text text-truncate" 
                                           t-att-title="lead.short_description">
                                            <t t-esc="lead.short_description"/>
                                        </p>
                                        
                                        <!-- Published Date -->
                                        <p class="text-muted small">
                                            <i class="fa fa-calendar"></i>
                                            Posted: 
                                            <t t-esc="lead.website_published_date.strftime('%d/%m/%Y')"/>
                                        </p>
                                        
                                        <!-- View Count -->
                                        <p class="text-muted small">
                                            <i class="fa fa-eye"></i>
                                            <t t-esc="lead.website_view_count"/> views
                                        </p>
                                        
                                    </div>
                                    
                                    <!-- Card Footer -->
                                    <div class="card-footer bg-white">
                                        <a t-attf-href="/leads/{lead.id}" 
                                           class="btn btn-primary btn-sm w-100">
                                            View Details →
                                        </a>
                                    </div>
                                    
                                </div>
                            </div>
                        </t>
                    </t>
                    <t t-else="">
                        <div class="col-12">
                            <div class="alert alert-info text-center">
                                <h4>No leads found</h4>
                                <p>Try adjusting your search filters</p>
                            </div>
                        </div>
                    </t>
                </div>
                
                <!-- Pagination -->
                <t t-if="total_pages > 1">
                    <nav class="mt-5">
                        <ul class="pagination justify-content-center">
                            
                            <!-- Previous -->
                            <li class="page-item" t-att-class="'disabled' if page == 1 else ''">
                                <a class="page-link" 
                                   t-attf-href="/leads?page={page - 1}">
                                    Previous
                                </a>
                            </li>
                            
                            <!-- Page numbers -->
                            <t t-foreach="range(1, total_pages + 1)" t-as="pg">
                                <li class="page-item" t-att-class="'active' if pg == page else ''">
                                    <a class="page-link" t-attf-href="/leads?page={pg}">
                                        <t t-esc="pg"/>
                                    </a>
                                </li>
                            </t>
                            
                            <!-- Next -->
                            <li class="page-item" 
                                t-att-class="'disabled' if page == total_pages else ''">
                                <a class="page-link" 
                                   t-attf-href="/leads?page={page + 1}">
                                    Next
                                </a>
                            </li>
                            
                        </ul>
                    </nav>
                </t>
                
            </div>
        </t>
    </template>
    
    <!-- Lead Detail Template -->
    <template id="lead_detail" name="Lead Detail">
        <t t-call="website.layout">
            <div class="container my-5">
                
                <!-- Back Link -->
                <div class="mb-4">
                    <a href="/leads" class="btn btn-outline-secondary">
                        ← Back to Leads
                    </a>
                </div>
                
                <div class="row">
                    
                    <!-- Main Content -->
                    <div class="col-lg-8">
                        
                        <!-- Lead Image -->
                        <div class="mb-4">
                            <t t-if="lead.lead_image">
                                <img t-attf-src="data:image/png;base64,{lead.lead_image}" 
                                     class="img-fluid rounded" 
                                     alt="Lead"/>
                            </t>
                        </div>
                        
                        <!-- Lead Info -->
                        <h1 class="mb-3"><t t-esc="lead.name"/></h1>
                        
                        <!-- Meta Info -->
                        <div class="row text-muted mb-4">
                            <div class="col-md-6">
                                <p>
                                    <i class="fa fa-map-marker"></i>
                                    <strong>Location:</strong>
                                    <t t-esc="lead.country_region"/>
                                </p>
                            </div>
                            <div class="col-md-6">
                                <p>
                                    <i class="fa fa-calendar"></i>
                                    <strong>Posted:</strong>
                                    <t t-esc="lead.website_published_date.strftime('%d/%m/%Y')"/>
                                </p>
                            </div>
                        </div>
                        
                        <!-- Category -->
                        <t t-if="lead.lead_category">
                            <p class="mb-4">
                                <span class="badge bg-primary p-2">
                                    <t t-esc="lead.lead_category.name"/>
                                </span>
                            </p>
                        </t>
                        
                        <!-- Full Description -->
                        <div class="mb-5">
                            <h3>Description</h3>
                            <div class="lead-full-description">
                                <t t-esc="lead.description or lead.short_description"/>
                            </div>
                        </div>
                        
                        <!-- Related Leads -->
                        <t t-if="related_leads">
                            <div class="mb-5">
                                <h3>Related Opportunities</h3>
                                <div class="row">
                                    <t t-foreach="related_leads" t-as="related">
                                        <div class="col-md-6 mb-3">
                                            <div class="card h-100">
                                                <t t-if="related.lead_image">
                                                    <img t-attf-src="data:image/png;base64,{related.lead_image}"
                                                         class="card-img-top"
                                                         alt="Related Lead"
                                                         style="height: 200px; object-fit: cover;"/>
                                                </t>
                                                <div class="card-body">
                                                    <h6 class="card-title">
                                                        <t t-esc="related.name"/>
                                                    </h6>
                                                    <p class="card-text small">
                                                        <t t-esc="related.short_description[:100]"/>...
                                                    </p>
                                                </div>
                                                <div class="card-footer bg-white">
                                                    <a t-attf-href="/leads/{related.id}"
                                                       class="btn btn-sm btn-primary">
                                                        View
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </t>
                        
                    </div>
                    
                    <!-- Sidebar -->
                    <div class="col-lg-4">
                        
                        <!-- Contact Form -->
                        <div class="card shadow-sm">
                            <div class="card-header bg-primary text-white">
                                <h5 class="mb-0">Contact Regarding This Lead</h5>
                            </div>
                            <div class="card-body">
                                <form id="lead_contact_form">
                                    
                                    <!-- Name -->
                                    <div class="mb-3">
                                        <label class="form-label">Your Name *</label>
                                        <input type="text" 
                                               name="contact_name" 
                                               class="form-control" 
                                               required/>
                                    </div>
                                    
                                    <!-- Email -->
                                    <div class="mb-3">
                                        <label class="form-label">Email Address *</label>
                                        <input type="email" 
                                               name="contact_email" 
                                               class="form-control" 
                                               required/>
                                    </div>
                                    
                                    <!-- Phone -->
                                    <div class="mb-3">
                                        <label class="form-label">Phone Number</label>
                                        <input type="tel" 
                                               name="contact_phone" 
                                               class="form-control"/>
                                    </div>
                                    
                                    <!-- Message -->
                                    <div class="mb-3">
                                        <label class="form-label">Message *</label>
                                        <textarea name="message" 
                                                  class="form-control" 
                                                  rows="5" 
                                                  required></textarea>
                                    </div>
                                    
                                    <!-- Submit -->
                                    <button type="submit" class="btn btn-success w-100">
                                        Send Inquiry
                                    </button>
                                    
                                </form>
                                
                                <!-- Form Status Messages -->
                                <div id="form_status_message" class="mt-3"></div>
                            </div>
                        </div>
                        
                        <!-- Contact Info if available -->
                        <t t-if="lead.contact_email or lead.contact_phone">
                            <div class="card shadow-sm mt-4">
                                <div class="card-header">
                                    <h5 class="mb-0">Quick Contact Info</h5>
                                </div>
                                <div class="card-body">
                                    <t t-if="lead.contact_person">
                                        <p>
                                            <strong>Contact:</strong>
                                            <t t-esc="lead.contact_person"/>
                                        </p>
                                    </t>
                                    <t t-if="lead.contact_email">
                                        <p>
                                            <strong>Email:</strong>
                                            <a t-attf-href="mailto:{lead.contact_email}">
                                                <t t-esc="lead.contact_email"/>
                                            </a>
                                        </p>
                                    </t>
                                    <t t-if="lead.contact_phone">
                                        <p>
                                            <strong>Phone:</strong>
                                            <a t-attf-href="tel:{lead.contact_phone}">
                                                <t t-esc="lead.contact_phone"/>
                                            </a>
                                        </p>
                                    </t>
                                </div>
                            </div>
                        </t>
                        
                        <!-- Stats -->
                        <div class="card shadow-sm mt-4">
                            <div class="card-body text-center">
                                <p class="mb-0 text-muted">
                                    <i class="fa fa-eye"></i>
                                    <strong><t t-esc="lead.website_view_count"/></strong> views
                                </p>
                                <p class="mb-0 text-muted">
                                    <i class="fa fa-comment"></i>
                                    <strong><t t-esc="lead.website_contact_count"/></strong> inquiries
                                </p>
                            </div>
                        </div>
                        
                    </div>
                    
                </div>
                
            </div>
        </t>
    </template>
    
</odoo>
```

#### 3.3 CSS Styling

```css
/* static/css/lead_cards.css */

/* Lead Cards */
.lead-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    overflow: hidden;
    border: 1px solid #e0e0e0;
}

.lead-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1) !important;
}

.lead-image-container {
    position: relative;
    height: 250px;
    overflow: hidden;
    background-color: #f5f5f5;
}

.lead-image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.lead-card:hover .lead-image-container img {
    transform: scale(1.05);
}

.lead-card .badge {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.lead-card .card-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: #1a1a1a;
    line-height: 1.4;
    min-height: 2.8em;
}

.lead-card .card-text {
    font-size: 0.95rem;
    line-height: 1.5;
    color: #666;
    min-height: 3em;
}

/* Lead Detail Page */
.lead-full-description {
    font-size: 1.05rem;
    line-height: 1.8;
    color: #333;
}

.lead-full-description p {
    margin-bottom: 1rem;
}

/* Contact Form */
#lead_contact_form {
    background-color: #f9f9f9;
}

#lead_contact_form .form-label {
    font-weight: 500;
    color: #333;
}

#lead_contact_form .form-control:focus {
    border-color: #007bff;
    box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
}

/* Filters */
.filters-section {
    background-color: #f8f9fa;
    padding: 1.5rem;
    border-radius: 0.5rem;
    margin-bottom: 2rem;
}

.filters-section .form-select,
.filters-section .form-control {
    border-radius: 0.375rem;
}

/* Pagination */
.pagination .page-link {
    color: #007bff;
    border: 1px solid #dee2e6;
}

.pagination .page-link:hover {
    color: #0056b3;
    background-color: #e9ecef;
}

.pagination .page-item.active .page-link {
    background-color: #007bff;
    border-color: #007bff;
}

/* Responsive */
@media (max-width: 768px) {
    .lead-card {
        margin-bottom: 1.5rem;
    }
    
    .lead-image-container {
        height: 200px;
    }
    
    .lead-card .card-title {
        font-size: 1rem;
    }
}
```

#### 3.4 JavaScript

```javascript
// static/js/lead_portal.js

document.addEventListener('DOMContentLoaded', function() {
    
    // Contact Form Handler
    const contactForm = document.getElementById('lead_contact_form');
    if (contactForm) {
        contactForm.addEventListener('submit', handleContactSubmit);
    }
    
});

async function handleContactSubmit(e) {
    e.preventDefault();
    
    const form = e.target;
    const leadId = new URLSearchParams(window.location.search).get('lead_id') || 
                   window.location.pathname.split('/').pop();
    
    // Get form data
    const formData = {
        contact_name: form.querySelector('input[name="contact_name"]').value,
        contact_email: form.querySelector('input[name="contact_email"]').value,
        contact_phone: form.querySelector('input[name="contact_phone"]').value,
        message: form.querySelector('textarea[name="message"]').value,
    };
    
    // Show loading state
    const submitBtn = form.querySelector('button[type="submit"]');
    const originalText = submitBtn.textContent;
    submitBtn.disabled = true;
    submitBtn.textContent = 'Sending...';
    
    try {
        // Send request
        const response = await fetch(`/leads/${leadId}/contact`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(formData),
        });
        
        const result = await response.json();
        
        // Show message
        const statusDiv = document.getElementById('form_status_message');
        if (result.status === 'success') {
            statusDiv.innerHTML = `
                <div class="alert alert-success alert-dismissible fade show">
                    ${result.message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
            form.reset();
        } else {
            statusDiv.innerHTML = `
                <div class="alert alert-danger alert-dismissible fade show">
                    ${result.message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
        }
        
    } catch (error) {
        const statusDiv = document.getElementById('form_status_message');
        statusDiv.innerHTML = `
            <div class="alert alert-danger alert-dismissible fade show">
                An error occurred. Please try again later.
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        `;
        console.error('Error:', error);
    } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = originalText;
    }
}
```

### Phase 4: Deployment & Testing (Tuần 3-4)

#### 4.1 Database Setup

```sql
-- Tạo lead categories mặc định
INSERT INTO lead_category (name, code, color, sequence) VALUES
('Real Estate', 'real-estate', '#FF6B6B', 10),
('Technology', 'technology', '#4ECDC4', 20),
('Manufacturing', 'manufacturing', '#45B7D1', 30),
('Services', 'services', '#FFA07A', 40),
('Import/Export', 'import-export', '#98D8C8', 50);
```

#### 4.2 Testing Checklist

```markdown
### Backend Testing
- [ ] CRM Lead model fields được thêm thành công
- [ ] Form view hiển thị tất cả fields mới
- [ ] Action "Phê duyệt" cập nhật trạng thái đúng
- [ ] Action "Từ chối" mở wizard và lưu lý do
- [ ] Email thông báo được gửi đến approvers
- [ ] Approval history được ghi lại chính xác
- [ ] Search/filter hoạt động trên list view

### Frontend Testing
- [ ] Trang /leads hiển thị leads được duyệt
- [ ] Filters (category, country, search) hoạt động
- [ ] Pagination hoạt động đúng
- [ ] Click vào card → lead detail page
- [ ] Lead detail page hiển thị đầy đủ thông tin
- [ ] Contact form submit tạo lead mới
- [ ] Email confirmation được gửi
- [ ] Related leads hiển thị đúng
- [ ] View count tăng khi truy cập
- [ ] Responsive trên mobile/tablet

### Security Testing
- [ ] Chỉ approved leads hiển thị trên website
- [ ] User không authenticated không thể submit contact form
- [ ] CSRF tokens hoạt động trên forms
- [ ] SQL injection tests
- [ ] XSS tests trên description fields
```

---

## ✅ Các Tính Năng Có Thể/Không Thể Thực Hiện

### 🟢 CÓ THỂ THỰC HIỆN

#### 1. **Quản Lý Lead Approval**
- ✅ Admin review và approve/reject leads
- ✅ Lịch sử duyệt chi tiết
- ✅ Gửi email thông báo
- ✅ Lý do từ chối
- ✅ Resubmit sau từ chối

#### 2. **Website Portal**
- ✅ Hiển thị leads dưới dạng cards
- ✅ Filter by category/country
- ✅ Search functionality
- ✅ Pagination
- ✅ Lead detail page
- ✅ Contact form trên detail page
- ✅ Responsive design

#### 3. **Data Management**
- ✅ Đồng bộ Lead từ CRM → Website
- ✅ Thống kê view count
- ✅ Thống kê contact count
- ✅ Quản lý lead images
- ✅ Multiple language support (với translation module)

#### 4. **Security & Access Control**
- ✅ Role-based access (Approvers vs regular users)
- ✅ View count tracking
- ✅ Activity logging
- ✅ Audit trail

#### 5. **Advanced Features**
- ✅ Lead recommendations (related leads)
- ✅ API endpoints cho 3rd party integration
- ✅ Analytics dashboard
- ✅ Bulk approval/rejection
- ✅ Email template customization

### 🔴 KHÔNG THỂ THỰC HIỆN (Limitations)

#### 1. **Real-time Notifications**
- ❌ Server-sent events (SSE) - Odoo không hỗ trợ tốt
- ❌ WebSocket connections - Cần setup riêng
- **Giải pháp:** Dùng email + periodic polling

#### 2. **Advanced Analytics**
- ❌ Real-time dashboard updates - Performance issues
- ❌ Complex ML-based recommendations - Out of scope
- **Giải pháp:** Dùng scheduled reports + export to BI tools

#### 3. **Multi-tenant Website**
- ❌ Hoàn toàn separate websites cho mỗi company
- **Giải pháp:** Dùng website filtering theo company

#### 4. **Direct Payment Integration**
- ❌ Direct payment cho leads (vì không bán hàng)
- ✅ Nhưng có thể integrate payment cho commission nếu cần

#### 5. **Mobile App**
- ❌ Native mobile app - không trong scope
- ✅ Responsive website thay thế

### 🟡 CÓ ĐIỀU KIỆN

#### 1. **Lead Duplication Detection**
- ⚠️ Có thể thực hiện nhưng cần setup rules
- Cần define duplicate criteria

#### 2. **Lead Scoring/Qualification**
- ⚠️ Có thể thực hiện nhưng cần business rules
- Cần custom Python code

#### 3. **Automated Workflows**
- ⚠️ Có thể thực hiện nhưng phức tạp
- Cần extra server processing

---

## 🔧 Bổ Sung và Tối Ưu

### Bổ Sung Cần Thiết

#### 1. **Security Enhancements**

```python
# models/security.py
class LeadSecurityMixin(models.AbstractModel):
    _name = 'lead.security.mixin'
    
    @api.model
    def check_access_write_website_fields(self, ids, operation='write'):
        """Kiểm tra quyền ghi website fields"""
        for record in self.browse(ids):
            if not self.env.user.has_group(
                'website_lead_showcase.group_lead_approver'
            ):
                if operation == 'write':
                    # Chỉ cho phép một số fields
                    writable_fields = ['short_description', 'lead_image']
                    # Check if trying to write protected fields
                    pass
```

#### 2. **Performance Optimization**

```python
# Thêm indexes cho database
# models.py
class CrmLead(models.Model):
    _inherit = 'crm.lead'
    
    # Add indexes
    _sql_constraints = [
        ('approval_status_check', 
         'CHECK(approval_status IN (\'draft\', \'approved\', \'rejected\'))',
         'Invalid approval status'),
    ]
    
    # Computed fields cần cache
    website_view_count = fields.Integer(
        compute='_compute_website_metrics',
        store=True,  # Store để query nhanh
    )
```

#### 3. **Email Template Customization**

```xml
<!-- views/email_templates.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="email_lead_approval_request" model="mail.template">
        <field name="name">Lead Approval Request</field>
        <field name="email_from">${(object.company_id or user.company_id).email}</field>
        <field name="subject">New Lead Needs Approval: ${object.name}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html"><![CDATA[
            <div style="font-family: Arial; font-size: 12px;">
                <p>Hello,</p>
                
                <p>A new lead has been submitted and requires your review:</p>
                
                <table style="border: 1px solid #ccc; padding: 10px;">
                    <tr><td><strong>Lead Name:</strong></td><td>${object.name}</td></tr>
                    <tr><td><strong>Category:</strong></td><td>${object.lead_category.name or 'N/A'}</td></tr>
                    <tr><td><strong>Country:</strong></td><td>${object.country_region or 'N/A'}</td></tr>
                    <tr><td><strong>Description:</strong></td><td>${object.short_description or 'N/A'}</td></tr>
                    <tr><td><strong>Submitted by:</strong></td><td>${object.create_uid.name}</td></tr>
                    <tr><td><strong>Date:</strong></td><td>${object.create_date.strftime('%d/%m/%Y %H:%M')}</td></tr>
                </table>
                
                <p>
                    <a href="${object.get_portal_url()}" style="background-color: #007bff; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px;">
                        Review Lead
                    </a>
                </p>
                
                <p>Thank you,<br/>CRM System</p>
            </div>
        ]]></field>
    </record>
    
    <record id="email_lead_approved" model="mail.template">
        <field name="name">Lead Approved</field>
        <field name="email_from">${(object.company_id or user.company_id).email}</field>
        <field name="subject">Your Lead Has Been Approved: ${object.name}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html"><![CDATA[
            <div style="font-family: Arial; font-size: 12px;">
                <p>Dear ${object.create_uid.name},</p>
                
                <p>Great news! Your lead "<strong>${object.name}</strong>" has been approved and is now visible on our website.</p>
                
                <p>
                    <a href="https://yourwebsite.com/leads/${object.id}">
                        View on Website
                    </a>
                </p>
                
                <p>Best regards,<br/>CRM Team</p>
            </div>
        ]]></field>
    </record>
    
    <record id="email_lead_rejected" model="mail.template">
        <field name="name">Lead Rejected</field>
        <field name="email_from">${(object.company_id or user.company_id).email}</field>
        <field name="subject">Your Lead Needs Revision: ${object.name}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html"><![CDATA[
            <div style="font-family: Arial; font-size: 12px;">
                <p>Hello ${object.create_uid.name},</p>
                
                <p>Your lead "<strong>${object.name}</strong>" has been reviewed and requires some revisions:</p>
                
                <p><strong>Reason:</strong></p>
                <p>${object.rejection_reason or 'Please see the details for more information'}</p>
                
                <p>Please update your lead and resubmit it for approval.</p>
                
                <p>Thank you!</p>
            </div>
        ]]></field>
    </record>
    
</odoo>
```

#### 4. **Lead Category Data**

```xml
<!-- data/lead_category_data.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="lead_category_real_estate" model="lead.category">
        <field name="name">Real Estate</field>
        <field name="code">real-estate</field>
        <field name="description">Real estate and property investment opportunities</field>
        <field name="icon">fa-building</field>
        <field name="color">#FF6B6B</field>
        <field name="sequence">10</field>
    </record>
    
    <record id="lead_category_technology" model="lead.category">
        <field name="name">Technology</field>
        <field name="code">technology</field>
        <field name="description">Tech startup and software opportunities</field>
        <field name="icon">fa-laptop</field>
        <field name="color">#4ECDC4</field>
        <field name="sequence">20</field>
    </record>
    
    <!-- Add more as needed -->
    
</odoo>
```

#### 5. **Access Control (Security)**

```xml
<!-- security/ir_model_access.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Groups -->
    <record id="group_lead_approver" model="res.groups">
        <field name="name">Lead Approvers</field>
        <field name="comment">Can approve/reject leads for website publication</field>
    </record>
    
    <record id="group_lead_manager" model="res.groups">
        <field name="name">Lead Managers</field>
        <field name="comment">Can manage all lead operations</field>
        <field name="implied_ids" eval="[(4, ref('group_lead_approver'))]"/>
    </record>
    
    <!-- Access Rules -->
    <record id="access_lead_category_user" model="ir.model.access">
        <field name="name">Lead Category User</field>
        <field name="model_id" ref="model_lead_category"/>
        <field name="group_id" ref="base.group_user"/>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">0</field>
        <field name="perm_unlink">0</field>
    </record>
    
    <record id="access_lead_approval_history_user" model="ir.model.access">
        <field name="name">Lead Approval History User</field>
        <field name="model_id" ref="model_lead_approval_history"/>
        <field name="group_id" ref="base.group_user"/>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">0</field>
        <field name="perm_unlink">0</field>
    </record>
    
    <!-- Approver Rules -->
    <record id="access_crm_lead_approver" model="ir.model.access">
        <field name="name">CRM Lead Approver</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="group_id" ref="group_lead_approver"/>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>
    
</odoo>
```

#### 6. **Database Migration Script**

```python
# models/migrations.py
"""
Migrations để update existing leads
"""

def migrate_existing_leads(cr, registry):
    """
    Chạy khi update module
    Set default values cho existing leads
    """
    cr.execute("""
        UPDATE crm_lead 
        SET approval_status = 'draft',
            website_visible = False
        WHERE approval_status IS NULL
    """)
```

### Tối Ưu Hóa

#### 1. **Query Optimization**

```python
# Bad query
leads = request.env['crm.lead'].search([
    ('approval_status', '=', 'approved'),
    ('website_visible', '=', True),
])
for lead in leads:
    category_name = lead.lead_category.name  # N+1 query

# Good query
leads = request.env['crm.lead'].search([
    ('approval_status', '=', 'approved'),
    ('website_visible', '=', True),
], order='website_published_date desc')

# Use read() with fields to avoid N+1
leads_data = leads.read([
    'name', 'lead_image', 'short_description',
    'lead_category', 'country_region', 'website_published_date'
])
```

#### 2. **Caching Strategy**

```python
# Add cache cho categories
@api.model
@tools.ormcache('self.env.user.id')
def get_lead_categories(self):
    return self.env['lead.category'].search([], order='sequence')
```

#### 3. **Async Task Processing**

```python
# Gửi email async thay vì đồng bộ
from odoo.addons.web_unseen.models.mail_notification import MailNotification

# Thêm vào manifest
'external_dependencies': {
    'python': ['celery'],  # Optional, for async tasks
}

# Dùng queue_job nếu cài sẵn
def _send_email_async(self):
    self.with_delay().action_send_email()
```

---

## 📝 Kết Luận

### Tóm Tắt Phương Án

**Kiến trúc:** Backend (Odoo CRM) + Frontend (Website Portal)
**Workflow:** Create → Submit → Approve → Publish → Display
**Key Features:** Lead approval workflow, website portal, contact forms, analytics
**Security:** Role-based access, audit logging, secure forms

### Timeline Đề Xuất

| Phase | Tuần | Công Việc | Người Thực Hiện |
|-------|------|----------|-----------------|
| Prep | 1 | Module setup, models | Backend Dev |
| Backend Dev | 1-2 | Views, controllers, emails | Backend Dev |
| Frontend | 2-3 | Website templates, JS, CSS | Frontend Dev |
| Testing | 3-4 | QA, debugging, optimization | QA/Dev |
| Deployment | 4 | Production setup, training | DevOps/PM |

### Estimasi Công Suất

- **Backend Development:** 40-50 hours
- **Frontend Development:** 30-40 hours
- **Testing & QA:** 20-25 hours
- **Documentation:** 10-15 hours
- **Total:** ~100-130 hours (2-3 weeks với full team)

### Nguy Hiểm Tiềm Tàng

1. **Email Delivery Issues** → Setup SPF/DKIM/DMARC
2. **Image Upload Size** → Implement image compression
3. **Performance at Scale** → Implement caching early
4. **Security Vulnerabilities** → Regular security audits

### Next Steps

1. Tạo module skeleton
2. Implement models + fields
3. Tạo backend views + approval workflow
4. Implement website portal controllers
5. Tạo templates + frontend styling
6. Comprehensive testing
7. Deploy to production
8. User training + documentation

----------------------------------------
FILE: ./Lead_Website_Odoo_Solution.md:Zone.Identifier
----------------------------------------

[ZoneTransfer]
ZoneId=3 
----------------------------------------
FILE: ./__init__.py
----------------------------------------

from . import models, controllers
from . import wizard

----------------------------------------
FILE: ./__manifest__.py
----------------------------------------

{
    'name': 'Website Lead Showcase',
    'version': '19.0.1.0.0',
    'category': 'Website',
    'sequence': 1,
    'author': 'Your Company',
    'depends': ['website', 'crm', 'website_crm', 'sale_crm'],
    'installable': True,
    'application': False,
    'data': [
        'views/website_lead_gallery_template.xml',
        'views/crm_lead_gallery_views.xml',
        'security/security_gallery.xml',
        'security/security_image.xml',
        # Security
        'security/ir_model_access.xml',
        
        # Data
        'data/lead_category_data.xml',
        'data/test_lead_data.xml',
        
        # Views - Backend
        'views/crm_lead_views.xml',
        'views/dashboard_views.xml',
        
        # Views - Frontend
        'views/website_templates.xml',
        'views/email_templates.xml',
        'views/lead_reject_wizard_views.xml',
    ],
    'assets': {
        'web.assets_frontend': [
            'website_lead_showcase/static/css/lead_cards.css',
            'website_lead_showcase/static/js/lead_portal.js',
        ],
    },
    'external_dependencies': {
        'python': [],
        'bin': [],
    },
}

----------------------------------------
FILE: ./controllers/__init__.py
----------------------------------------

from . import main
from . import home_override

----------------------------------------
FILE: ./controllers/home_override.py
----------------------------------------

from odoo import http
from odoo.http import request
from odoo.addons.website.controllers.main import Website

class WebsiteLeadRedirect(Website):
    @http.route('/', type='http', auth="public", website=True, sitemap=True)
    def index(self, **kw):
        # Ép chuyển hướng từ trang chủ trắng tinh sang trang danh sách Lead
        return request.redirect('/leads')

----------------------------------------
FILE: ./controllers/main.py
----------------------------------------

import json
from odoo import http, fields
from odoo.http import request

class LeadPortalController(http.Controller):
    
    @http.route('/leads', auth='public', website=True, sitemap=True)
    def leads_list(self, **kwargs):
        category_id = kwargs.get('category_id')
        country = kwargs.get('country')
        search_query = kwargs.get('search')
        intent = kwargs.get('intent')
        page = int(kwargs.get('page', 1))
        per_page = 12
        
        domain = [('approval_status', '=', 'approved'), ('website_visible', '=', True)]
        
        if intent:
            domain.append(('lead_intent', '=', intent))
        if category_id:
            domain.append(('lead_category', '=', int(category_id)))
        if country:
            domain.append(('country_region', 'ilike', country))
        if search_query:
            domain.append(('name', 'ilike', search_query))
        
        leads = request.env['crm.lead'].sudo().search(domain, order='website_published_date desc')
        total = len(leads)
        
        categories = request.env['lead.category'].sudo().search([])
        countries = request.env['crm.lead'].sudo().search([('approval_status', '=', 'approved')]).mapped('country_region')
        
        return request.render('website_lead_showcase.leads_list', {
            'leads': leads[((page - 1) * per_page):(page * per_page)],
            'categories': categories,
            'countries': sorted(list(set([c for c in countries if c]))),
            'total': total,
            'page': page,
            'per_page': per_page,
            'total_pages': (total + per_page - 1) // per_page if per_page else 1,
            'search_query': search_query,
            'selected_category': category_id,
            'selected_country': country,
            'selected_intent': intent,
        })
    
    @http.route('/leads/<int:lead_id>', auth='public', website=True)
    def lead_detail(self, lead_id, **kwargs):
        lead = request.env['crm.lead'].sudo().browse(lead_id)
        if not lead.exists() or lead.approval_status != 'approved' or not lead.website_visible:
            return request.redirect('/leads')
        
        lead.sudo().website_view_count += 1
        related_leads = request.env['crm.lead'].sudo().search([
            ('approval_status', '=', 'approved'), ('website_visible', '=', True),
            ('lead_category', '=', lead.lead_category.id), ('id', '!=', lead_id)
        ], limit=4)
        
        return request.render('website_lead_showcase.lead_detail', {
            'lead': lead,
            'related_leads': related_leads,
        })
    
    @http.route('/leads/<int:lead_id>/contact', type='http', auth='public', methods=['POST'], csrf=False)
    def lead_contact(self, lead_id, **kwargs):
        lead = request.env['crm.lead'].sudo().browse(lead_id)
        if not lead.exists() or lead.approval_status != 'approved':
            return request.make_response(json.dumps({'status': 'error', 'message': 'Lead not found'}), headers=[('Content-Type', 'application/json')])
        
        contact_name = kwargs.get('contact_name')
        contact_email = kwargs.get('contact_email')
        contact_phone = kwargs.get('contact_phone', '')
        message = kwargs.get('message')
        
        if not all([contact_name, contact_email, message]):
            return request.make_response(json.dumps({'status': 'error', 'message': 'Thiếu thông tin bắt buộc!'}), headers=[('Content-Type', 'application/json')])
        
        new_lead = request.env['crm.lead'].sudo().create({
            'name': f"[YÊU CẦU KẾT NỐI] {contact_name} -> {lead.name}",
            'email_from': contact_email,
            'phone': contact_phone,
            'description': message,
            'type': 'opportunity',
            'referred_lead_id': lead.id,
            'approval_status': 'draft',
            'website_visible': False,
        })
        lead.sudo().website_contact_count += 1
        
        template = request.env.ref('website_lead_showcase.email_lead_inquiry', raise_if_not_found=False)
        if template:
            template.sudo().send_mail(new_lead.id, force_send=False)
            
        return request.make_response(json.dumps({'status': 'success', 'message': 'Cảm ơn! Chúng tôi sẽ liên hệ với bạn sớm nhất.'}), headers=[('Content-Type', 'application/json')])

----------------------------------------
FILE: ./data/lead_category_data.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="lead_category_real_estate" model="lead.category">
        <field name="name">Real Estate</field>
        <field name="code">real-estate</field>
        <field name="description">Real estate and property investment opportunities</field>
        <field name="icon">fa-building</field>
        <field name="color">#FF6B6B</field>
        <field name="sequence">10</field>
    </record>
    
    <record id="lead_category_technology" model="lead.category">
        <field name="name">Technology</field>
        <field name="code">technology</field>
        <field name="description">Tech startup and software opportunities</field>
        <field name="icon">fa-laptop</field>
        <field name="color">#4ECDC4</field>
        <field name="sequence">20</field>
    </record>
    
    <record id="lead_category_manufacturing" model="lead.category">
        <field name="name">Manufacturing</field>
        <field name="code">manufacturing</field>
        <field name="description">Manufacturing and production opportunities</field>
        <field name="icon">fa-industry</field>
        <field name="color">#45B7D1</field>
        <field name="sequence">30</field>
    </record>
    
    <record id="lead_category_services" model="lead.category">
        <field name="name">Services</field>
        <field name="code">services</field>
        <field name="description">Professional services and consulting</field>
        <field name="icon">fa-briefcase</field>
        <field name="color">#FFA07A</field>
        <field name="sequence">40</field>
    </record>
    
    <record id="lead_category_import_export" model="lead.category">
        <field name="name">Import/Export</field>
        <field name="code">import-export</field>
        <field name="description">Trade and import/export opportunities</field>
        <field name="icon">fa-ship</field>
        <field name="color">#98D8C8</field>
        <field name="sequence">50</field>
    </record>
    
</odoo>

----------------------------------------
FILE: ./data/test_lead_data.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!-- Lead 1: Đã duyệt (Hiện trên Website) -->
    <record id="test_lead_tech_1" model="crm.lead">
        <field name="name">Tìm đối tác phát triển phần mềm ERP</field>
        <field name="type">opportunity</field>
        <field name="lead_category" ref="website_lead_showcase.lead_category_technology"/>
        <field name="country_region">Việt Nam</field>
        <field name="short_description">Cần tìm đội ngũ lập trình có kinh nghiệm triển khai ERP cho ngành bán lẻ.</field>
        <field name="description">Chúng tôi đang mở rộng chuỗi cửa hàng bán lẻ và cần xây dựng một hệ thống ERP nội bộ. Yêu cầu công nghệ hiện đại, có khả năng mở rộng.</field>
        <field name="contact_person">Nguyễn Văn A</field>
        <field name="contact_email">nguyenvana@example.com</field>
        <field name="approval_status">approved</field>
        <field name="website_visible">True</field>
        <field name="website_published_date" eval="time.strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="lead_image">iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==</field>
    </record>

    <!-- Lead 2: Đã duyệt (Hiện trên Website) -->
    <record id="test_lead_export_1" model="crm.lead">
        <field name="name">Nguồn cung Nông sản xuất khẩu Nhật Bản</field>
        <field name="type">opportunity</field>
        <field name="lead_category" ref="website_lead_showcase.lead_category_import_export"/>
        <field name="country_region">Nhật Bản</field>
        <field name="short_description">Tìm nguồn cung cấp hạt điều, cà phê chất lượng cao xuất sang thị trường Nhật.</field>
        <field name="description">Doanh nghiệp chúng tôi tại Tokyo đang tìm kiếm các nhà cung cấp nông sản uy tín tại Việt Nam để hợp tác dài hạn.</field>
        <field name="contact_person">Trần Thị B</field>
        <field name="contact_email">tranthib_jp@example.com</field>
        <field name="approval_status">approved</field>
        <field name="website_visible">True</field>
        <field name="website_published_date" eval="time.strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="lead_image">iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==</field>
    </record>

    <!-- Lead 3: Chưa duyệt (Để test quy trình Admin) -->
    <record id="test_lead_services_1" model="crm.lead">
        <field name="name">Cần tư vấn Pháp lý mở chi nhánh</field>
        <field name="type">opportunity</field>
        <field name="lead_category" ref="website_lead_showcase.lead_category_services"/>
        <field name="country_region">Singapore</field>
        <field name="short_description">Tìm công ty luật hỗ trợ mở chi nhánh công ty tại Singapore.</field>
        <field name="approval_status">draft</field>
        <field name="website_visible">False</field>
    </record>
</odoo>

----------------------------------------
FILE: ./models/__init__.py
----------------------------------------

from . import lead_category
from . import approval_history
from . import crm_lead_extension
from . import crm_lead_image
from . import crm_lead_gallery

----------------------------------------
FILE: ./models/approval_history.py
----------------------------------------

from odoo import models, fields

class LeadApprovalHistory(models.Model):
    _name = 'lead.approval.history'
    _description = 'Lịch sử duyệt Lead'
    _order = 'create_date desc'
    
    lead_id = fields.Many2one('crm.lead', string="Lead", required=True)
    action = fields.Selection([
        ('submitted', 'Submit'),
        ('approved', 'Phê duyệt'),
        ('rejected', 'Từ chối'),
        ('resubmitted', 'Gửi lại'),
    ], string="Hành động", required=True)
    
    performed_by = fields.Many2one('res.users', string="Người thực hiện", default=lambda self: self.env.user.id)
    notes = fields.Text(string="Ghi chú")

----------------------------------------
FILE: ./models/crm_lead_extension.py
----------------------------------------

from odoo import models, fields, api
from odoo.exceptions import ValidationError

class CrmLead(models.Model):
    _inherit = 'crm.lead'
    
    # ===== TRƯỜNG BỔ SUNG =====
    referred_lead_id = fields.Many2one('crm.lead', string="Lead nguồn từ Website", readonly=True, help="Khách hàng liên hệ từ Lead này")
    
    # ===== FIELDS HIỂN THỊ WEBSITE =====
    website_visible = fields.Boolean(
        string="Hiển thị trên Website",
        default=False,
        help="Có hiển thị lead này trên website công khai hay không"
    )
    
    approval_status = fields.Selection([
        ('draft', 'Chưa duyệt'),
        ('approved', 'Đã duyệt'),
        ('rejected', 'Từ chối'),
    ], string="Trạng thái duyệt", default='draft')
    
    approval_date = fields.Datetime(
        string="Ngày duyệt",
        readonly=True,
        help="Thời gian Admin phê duyệt lead"
    )
    
    approved_by = fields.Many2one(
        'res.users',
        string="Người duyệt",
        readonly=True
    )
    
    rejection_reason = fields.Text(
        string="Lý do từ chối",
        help="Lý do nếu admin từ chối duyệt lead"
    )
    
    # ===== FIELDS HIỂN THỊ TRÊN CARD =====
    lead_image = fields.Image(
        string="Hình ảnh Lead",
        help="Ảnh đại diện cho lead trên website"
    )
    
    
    lead_intent = fields.Selection([
        ('buy', 'TÌM MUA'),
        ('sell', 'CHÀO BÁN'),
        ('partner', 'TÌM ĐỐI TÁC')
    ], string="Phân loại nhu cầu", default='buy')
    
    lead_category = fields.Many2one(
        'lead.category',
        string="Loại nhu cầu/Lĩnh vực",
        help="Phân loại nhu cầu của lead"
    )
    
    country_region = fields.Char(
        string="Quốc gia/Khu vực",
        help="Nơi lead đó cần hỗ trợ"
    )
    
    short_description = fields.Text(
        string="Mô tả ngắn",
        help="Mô tả tóm tắt về lead (hiển thị trên card)"
    )
    
    website_published_date = fields.Datetime(
        string="Ngày xuất bản",
        readonly=True,
        help="Thời gian lead được xuất bản trên website"
    )
    
    contact_person = fields.Char(
        string="Người liên hệ",
        help="Tên người liên hệ"
    )
    
    contact_phone = fields.Char(
        string="Số điện thoại liên hệ",
        help="Điện thoại để liên hệ"
    )
    
    contact_email = fields.Char(
        string="Email liên hệ",
        help="Email để liên hệ"
    )
    
    # ===== FIELDS THỐNG KÊ =====
    website_view_count = fields.Integer(
        string="Lượt xem trên website",
        default=0,
        readonly=True
    )
    
    website_contact_count = fields.Integer(
        string="Số lần được liên hệ từ website",
        default=0,
        readonly=True
    )
    
    # ===== WORKFLOW FIELDS =====
    last_approval_action = fields.Selection([
        ('submitted', 'Vừa được submit'),
        ('reviewed', 'Đã được review'),
        ('approved', 'Đã duyệt'),
        ('rejected', 'Bị từ chối'),
        ('resubmitted', 'Được resubmit sau từ chối'),
    ], string="Hành động duyệt cuối cùng", readonly=True)
    
    approval_history = fields.One2many(
        'lead.approval.history',
        'lead_id',
        string="Lịch sử duyệt",
        readonly=True
    )
    
    @api.model
    def create(self, vals):
        """Override create để tự động ghi approval history"""
        record = super().create(vals)
        
        # Tạo approval history record
        self.env['lead.approval.history'].create({
            'lead_id': record.id,
            'action': 'submitted',
            'performed_by': self.env.user.id,
            'notes': f"Lead được tạo bởi {self.env.user.name}",
        })
        
        # Gửi email thông báo đến approvers
        record._send_approval_notification(record)
        
        return record
    
    def action_approve(self):
        """Phê duyệt Lead"""
        # Check permission
        if not self.env.user.has_group('website_lead_showcase.group_lead_approver'):
            raise ValidationError("Bạn không có quyền phê duyệt Lead")
        
        # Check business rules
        if self.approval_status == 'approved':
            raise ValidationError("Lead này đã được phê duyệt rồi")
        
        if not self.lead_image:
            raise ValidationError("Lead phải có hình ảnh trước khi phê duyệt")
        
        if not self.short_description:
            raise ValidationError("Lead phải có mô tả trước khi phê duyệt")
        
        # Update lead
        self.write({
            'approval_status': 'approved',
            'website_visible': True,
            'website_published_date': fields.Datetime.now(),
            'approved_by': self.env.user.id,
            'approval_date': fields.Datetime.now(),
        })
        
        # Record history
        self.env['lead.approval.history'].create({
            'lead_id': self.id,
            'action': 'approved',
            'performed_by': self.env.user.id,
            'notes': 'Lead được phê duyệt và xuất bản trên website',
        })
        
        # Send confirmation email
        self._send_approval_confirmation_email()
    
    def action_reject(self):
        """Từ chối Lead"""
        return {
            'type': 'ir.actions.act_window',
            'res_model': 'lead.reject.wizard',
            'view_mode': 'form',
            'target': 'new',
            'context': {'default_lead_id': self.id},
        }
    
    def action_resubmit(self):
        """Gửi lại Lead"""
        if self.approval_status != 'rejected':
            raise ValidationError("Chỉ có thể gửi lại Lead bị từ chối")
        
        self.write({
            'approval_status': 'draft',
            'rejection_reason': '',
        })
        
        self.env['lead.approval.history'].create({
            'lead_id': self.id,
            'action': 'resubmitted',
            'performed_by': self.env.user.id,
        })
        
        self._send_approval_notification(self)
    
    def _send_approval_notification(self, record):
        """Gửi email thông báo đến approvers"""
        template = self.env.ref(
            'website_lead_showcase.email_lead_approval_request', raise_if_not_found=False
        )
        if template:
            approvers = self.env['res.users'].search([
                ('group_ids', 'in', 
                 self.env.ref('website_lead_showcase.group_lead_approver').id)
            ])
            for approver in approvers:
                template.send_mail(record.id, force_send=False)
    
    def _send_approval_confirmation_email(self):
        """Gửi email xác nhận phê duyệt"""
        template = self.env.ref(
            'website_lead_showcase.email_lead_approved', raise_if_not_found=False
        )
        if template:
            template.send_mail(self.id, force_send=False)
    
    @api.constrains('lead_image', 'short_description')
    def _check_required_fields_for_approval(self):
        """Kiểm tra fields bắt buộc khi phê duyệt"""
        for record in self:
            if record.approval_status == 'approved':
                if not record.lead_image:
                    raise ValidationError(
                        f"Lead '{record.name}' phải có hình ảnh"
                    )
                if not record.short_description:
                    raise ValidationError(
                        f"Lead '{record.name}' phải có mô tả"
                    )

----------------------------------------
FILE: ./models/crm_lead_gallery.py
----------------------------------------

from odoo import models, fields

class LeadGalleryImage(models.Model):
    _name = 'lead.gallery.image'
    _description = 'Thư viện ảnh chi tiết Lead'
    
    lead_id = fields.Many2one('crm.lead', string='Lead', ondelete='cascade')
    image = fields.Image('Hình ảnh', required=True, max_width=1200, max_height=1200)
    name = fields.Char('Miêu tả / Tên sản phẩm', default='Ảnh sản phẩm')
    mimetype = fields.Char('Mimetype', default='image/jpeg')

class CrmLeadGallery(models.Model):
    _inherit = 'crm.lead'
    
    gallery_image_ids = fields.One2many('lead.gallery.image', 'lead_id', string='Thư viện ảnh Sản phẩm / Nhà sản xuất')

----------------------------------------
FILE: ./models/crm_lead_image.py
----------------------------------------

from odoo import models, fields

class LeadImage(models.Model):
    _name = 'lead.image'
    _description = 'Thư viện ảnh Lead'
    
    lead_id = fields.Many2one('crm.lead', string='Lead', ondelete='cascade')
    image = fields.Image('Ảnh', required=True)
    name = fields.Char('Mô tả ảnh')

class CrmLeadImageExtension(models.Model):
    _inherit = 'crm.lead'
    
    image_ids = fields.One2many('lead.image', 'lead_id', string='Thư viện ảnh chi tiết')

----------------------------------------
FILE: ./models/lead_category.py
----------------------------------------

from odoo import models, fields

class LeadCategory(models.Model):
    _name = 'lead.category'
    _description = 'Loại nhu cầu / Lĩnh vực Lead'
    
    name = fields.Char(string="Tên loại", required=True)
    code = fields.Char(string="Mã loại", required=True, unique=True)
    description = fields.Text(string="Mô tả")
    icon = fields.Char(string="Icon", help="Font Awesome icon class")
    color = fields.Char(string="Màu sắc", help="Hex color code")
    sequence = fields.Integer(string="Thứ tự", default=10)
    active = fields.Boolean(default=True)
    
    lead_ids = fields.One2many('crm.lead', 'lead_category', string="Leads")
    lead_count = fields.Integer(
        string="Số Lead",
        compute='_compute_lead_count'
    )
    
    def _compute_lead_count(self):
        for category in self:
            category.lead_count = len(category.lead_ids.filtered(
                lambda l: l.approval_status == 'approved'
            ))

----------------------------------------
FILE: ./security/ir.model.access.csv
----------------------------------------


access_lead_image_public,lead.image.public,model_lead_image,,1,0,0,0
access_lead_image_user,lead.image.user,model_lead_image,base.group_user,1,1,1,1

----------------------------------------
FILE: ./security/ir_model_access.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Groups -->
    <record id="group_lead_approver" model="res.groups">
        <field name="name">Lead Approvers</field>
        <field name="comment">Can approve/reject leads for website publication</field>
    </record>
    
    <record id="group_lead_manager" model="res.groups">
        <field name="name">Lead Managers</field>
        <field name="comment">Can manage all lead operations</field>
        <field name="implied_ids" eval="[(4, ref('group_lead_approver'))]"/>
    </record>
    
    <!-- Access Rules -->
    <record id="access_lead_category_user" model="ir.model.access">
        <field name="name">Lead Category User</field>
        <field name="model_id" ref="model_lead_category"/>
        <field name="group_id" ref="base.group_user"/>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">0</field>
        <field name="perm_unlink">0</field>
    </record>
    
    <record id="access_lead_approval_history_user" model="ir.model.access">
        <field name="name">Lead Approval History User</field>
        <field name="model_id" ref="model_lead_approval_history"/>
        <field name="group_id" ref="base.group_user"/>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>
    
    <!-- Approver Rules -->
    <record id="access_crm_lead_approver" model="ir.model.access">
        <field name="name">CRM Lead Approver</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="group_id" ref="group_lead_approver"/>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>
    
    <record id="access_lead_reject_wizard" model="ir.model.access">
        <field name="name">Lead Reject Wizard Access</field>
        <field name="model_id" ref="model_lead_reject_wizard"/>
        <field name="group_id" ref="group_lead_approver"/>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>

</odoo>

----------------------------------------
FILE: ./security/security_gallery.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="access_lead_gallery_image_public" model="ir.model.access">
            <field name="name">access.lead.gallery.image.public</field>
            <field name="model_id" ref="model_lead_gallery_image"/>
            <field name="perm_read">1</field>
            <field name="perm_write">0</field>
            <field name="perm_create">0</field>
            <field name="perm_unlink">0</field>
        </record>
        <record id="access_lead_gallery_image_user" model="ir.model.access">
            <field name="name">access.lead.gallery.image.user</field>
            <field name="model_id" ref="model_lead_gallery_image"/>
            <field name="group_id" ref="base.group_user"/>
            <field name="perm_read">1</field>
            <field name="perm_write">1</field>
            <field name="perm_create">1</field>
            <field name="perm_unlink">1</field>
        </record>
    </data>
</odoo>

----------------------------------------
FILE: ./security/security_image.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Quyền cho khách vãng lai (Chỉ xem) -->
        <record id="access_lead_image_public_xml" model="ir.model.access">
            <field name="name">access.lead.image.public.xml</field>
            <field name="model_id" ref="model_lead_image"/>
            <field name="perm_read">1</field>
            <field name="perm_write">0</field>
            <field name="perm_create">0</field>
            <field name="perm_unlink">0</field>
        </record>
        
        <!-- Quyền cho nhân viên nội bộ (Toàn quyền) -->
        <record id="access_lead_image_user_xml" model="ir.model.access">
            <field name="name">access.lead.image.user.xml</field>
            <field name="model_id" ref="model_lead_image"/>
            <field name="group_id" ref="base.group_user"/>
            <field name="perm_read">1</field>
            <field name="perm_write">1</field>
            <field name="perm_create">1</field>
            <field name="perm_unlink">1</field>
        </record>
    </data>
</odoo>
----------------------------------------
FILE: ./static/css/lead_cards.css
----------------------------------------

/* static/css/lead_cards.css */

/* Lead Cards */
.lead-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    overflow: hidden;
    border: 1px solid #e0e0e0;
}

.lead-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1) !important;
}

.lead-image-container {
    position: relative;
    height: 250px;
    overflow: hidden;
    background-color: #f5f5f5;
}

.lead-image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
}

.lead-card:hover .lead-image-container img {
    transform: scale(1.05);
}

.lead-card .badge {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.lead-card .card-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: #1a1a1a;
    line-height: 1.4;
    min-height: 2.8em;
}

.lead-card .card-text {
    font-size: 0.95rem;
    line-height: 1.5;
    color: #666;
    min-height: 3em;
}

/* Lead Detail Page */
.lead-full-description {
    font-size: 1.05rem;
    line-height: 1.8;
    color: #333;
}

.lead-full-description p {
    margin-bottom: 1rem;
}

/* Contact Form */
#lead_contact_form {
    background-color: #f9f9f9;
}

#lead_contact_form .form-label {
    font-weight: 500;
    color: #333;
}

#lead_contact_form .form-control:focus {
    border-color: #007bff;
    box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
}

/* Filters */
.filters-section {
    background-color: #f8f9fa;
    padding: 1.5rem;
    border-radius: 0.5rem;
    margin-bottom: 2rem;
}

.filters-section .form-select,
.filters-section .form-control {
    border-radius: 0.375rem;
}

/* Pagination */
.pagination .page-link {
    color: #007bff;
    border: 1px solid #dee2e6;
}

.pagination .page-link:hover {
    color: #0056b3;
    background-color: #e9ecef;
}

.pagination .page-item.active .page-link {
    background-color: #007bff;
    border-color: #007bff;
}

/* Responsive */
@media (max-width: 768px) {
    .lead-card {
        margin-bottom: 1.5rem;
    }
    
    .lead-image-container {
        height: 200px;
    }
    
    .lead-card .card-title {
        font-size: 1rem;
    }
}

----------------------------------------
FILE: ./static/js/lead_portal.js
----------------------------------------

document.addEventListener('DOMContentLoaded', function() {
    const contactForm = document.getElementById('lead_contact_form');
    if (contactForm) {
        contactForm.addEventListener('submit', handleContactSubmit);
    }
});

async function handleContactSubmit(e) {
    e.preventDefault();
    const form = e.target;
    const parts = window.location.pathname.split('/');
    const leadId = parts[parts.length - 1];
    
    const submitBtn = form.querySelector('button[type="submit"]');
    const originalText = submitBtn.textContent;
    submitBtn.disabled = true;
    submitBtn.textContent = 'Đang gửi...';
    
    try {
        const formData = new FormData(form);
        const response = await fetch(`/leads/${leadId}/contact`, {
            method: 'POST',
            body: formData
        });
        
        const result = await response.json();
        const statusDiv = document.getElementById('form_status_message');
        
        if (result.status === 'success') {
            statusDiv.innerHTML = `<div class="alert alert-success mt-3">${result.message}</div>`;
            form.reset();
        } else {
            statusDiv.innerHTML = `<div class="alert alert-danger mt-3">${result.message}</div>`;
        }
    } catch (error) {
        document.getElementById('form_status_message').innerHTML = `
            <div class="alert alert-danger mt-3">Lỗi kết nối. Vui lòng tải lại trang và thử lại!</div>`;
    } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = originalText;
    }
}

----------------------------------------
FILE: ./views/crm_lead_gallery_views.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_view_form_gallery_inherit" model="ir.ui.view">
        <field name="name">crm.lead.form.gallery.inherit</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Ảnh sản phẩm &amp; Nhà sản xuất" name="product_images">
                    <field name="gallery_image_ids" mode="kanban" widget="many2many_kanban">
                        <kanban>
                            <field name="id"/>
                            <field name="name"/>
                            <field name="image"/>
                            <templates>
                                <t t-name="kanban-box">
                                    <div class="oe_kanban_global_click card m-1 p-2" style="width: 150px; display: inline-block; vertical-align: top;">
                                        <div class="o_kanban_image text-center mb-1" style="height: 100px; overflow: hidden;">
                                            <img t-attf-src="/web/image/lead.gallery.image/#{record.id.raw_value}/image" class="w-100 h-100" style="object-fit: cover; border-radius: 4px;" alt="Ảnh"/>
                                        </div>
                                        <div class="oe_kanban_details text-center">
                                            <strong class="o_kanban_record_title text-truncate d-block" style="font-size: 12px;"><field name="name"/></strong>
                                            <a t-if="widget.deletable" type="delete" class="text-danger small mt-1 d-inline-block"><i class="fa fa-trash"/> Xóa ảnh này</a>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                        <form string="Chi tiết ảnh sản phẩm">
                            <sheet>
                                <group>
                                    <field name="name"/>
                                    <field name="image" widget="image" class="oe_avatar" options="{'preview_image': 'image'}"/>
                                </group>
                            </sheet>
                        </form>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>

----------------------------------------
FILE: ./views/crm_lead_views.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Form View riêng cho quản lý Website Lead -->
    <record id="crm_lead_view_form_website_showcase" model="ir.ui.view">
        <field name="name">crm.lead.form.website.showcase</field>
        <field name="model">crm.lead</field>
        <field name="arch" type="xml">
            <form string="Lead Website Management">
                <header>
                    <button name="action_approve" 
                            type="object" 
                            class="oe_highlight" 
                            string="Phê duyệt"
                            invisible="approval_status == 'approved'"/>
                    <button name="action_reject" 
                            type="object" 
                            string="Từ chối"
                            invisible="approval_status in ['approved', 'rejected']"/>
                    <button name="action_resubmit" 
                            type="object" 
                            string="Gửi lại"
                            invisible="approval_status != 'rejected'"/>
                    <field name="approval_status" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Tên Lead..."/></h1>
                    </div>
                    <group>
                        <group string="Trạng thái &amp; Duyệt">
                            <field name="website_visible"/>
                            <field name="approved_by" readonly="1"/>
                            <field name="approval_date" readonly="1"/>
                        </group>
                        <group string="Thông tin hiển thị Website">
                            <field name="lead_image" widget="image" options="{'size': [256, 256]}"/>
                            <field name="lead_intent" widget="radio" options="{'horizontal': true}"/>
                            <field name="lead_category"/>
                            <field name="country_region"/>
                            <field name="website_published_date" readonly="1"/>
                        </group>
                    </group>
                    
                    <group string="Mô tả">
                        <field name="short_description" nolabel="1" placeholder="Mô tả ngắn hiển thị trên card..."/>
                        <field name="description" placeholder="Mô tả chi tiết..."/>
                    </group>
                    
                    <group string="Thông tin liên hệ">
                        <field name="contact_person"/>
                        <field name="contact_phone"/>
                        <field name="contact_email"/>
                    </group>
                    
                    <group string="Thống kê Website">
                        <field name="website_view_count" readonly="1"/>
                        <field name="website_contact_count" readonly="1"/>
                        <field name="referred_lead_id"/>
                    </group>
                    
                    <group string="Lý do từ chối" invisible="approval_status != 'rejected'">
                        <field name="rejection_reason" nolabel="1"/>
                    </group>
                    
                    <notebook>
                        <page string="Lịch sử duyệt" name="approval_history_page">
                            <field name="approval_history" readonly="1">
                                <list create="0" delete="0">
                                    <field name="create_date"/>
                                    <field name="action"/>
                                    <field name="performed_by"/>
                                    <field name="notes"/>
                                </list>
                            </field>
                        </page>
                    
                        
    
                        
                        <page string="Ảnh sản phẩm &amp; Nhà sản xuất" name="product_images">
                            <group string="Quản lý thư viện ảnh sản phẩm &amp; nhà sản xuất (Có thể thêm nhiều ảnh và ghi miêu tả)">
                                <field name="gallery_image_ids" mode="list">
                                    <list editable="bottom">
                                        <field name="image" widget="image" options="{'size': [80, 80]}" string="Hình ảnh"/>
                                        <field name="name" string="Miêu tả / Tên sản phẩm"/>
                                    </list>
                                </field>
                            </group>
                        </page>

                        </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View cho quản lý Lead -->
    <record id="crm_lead_tree_view_website" model="ir.ui.view">
        <field name="name">crm.lead.tree.website</field>
        <field name="model">crm.lead</field>
        <field name="arch" type="xml">
            <list string="Leads - Website Management"
                  decoration-success="approval_status=='approved'"
                  decoration-warning="approval_status=='draft'"
                  decoration-danger="approval_status=='rejected'">
                <field name="name"/>
                <field name="lead_intent" widget="radio" options="{'horizontal': true}"/>
                            <field name="lead_category"/>
                <field name="approval_status" widget="badge"/>
                <field name="website_visible"/>
                <field name="approval_date"/>
                <field name="website_view_count"/>
            </list>
        </field>
    </record>
    
    <!-- Search View -->
    <record id="crm_lead_search_view_website" model="ir.ui.view">
        <field name="name">crm.lead.search.website</field>
        <field name="model">crm.lead</field>
        <field name="arch" type="xml">
            <search string="Leads">
                <field name="name" string="Lead Name"/>
                <field name="lead_intent" widget="radio" options="{'horizontal': true}"/>
                            <field name="lead_category"/>
                <field name="approval_status"/>
                <separator/>
                <filter name="approved" 
                        string="Approved" 
                        domain="[('approval_status', '=', 'approved')]"/>
                <filter name="draft" 
                        string="Draft" 
                        domain="[('approval_status', '=', 'draft')]"/>
                <filter name="rejected" 
                        string="Rejected" 
                        domain="[('approval_status', '=', 'rejected')]"/>
                <separator/>
                <filter name="website_visible" 
                        string="Visible on Website" 
                        domain="[('website_visible', '=', True)]"/>
            </search>
        </field>
    </record>
    
</odoo>

----------------------------------------
FILE: ./views/dashboard_views.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Action for Lead Management -->
    <record id="action_crm_lead_website_showcase" model="ir.actions.act_window">
        <field name="name">Website Leads</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('crm_lead_tree_view_website')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('crm_lead_view_form_website_showcase')})
        ]"/>
        <field name="search_view_id" ref="crm_lead_search_view_website"/>
    </record>

    <!-- Top-level Menu để dễ nhìn thấy trên giao diện -->
    <menuitem id="menu_lead_website_root" 
              name="Website Leads" 
              sequence="20"/>
    
    <menuitem id="menu_lead_approval" 
              name="Leads Showcase" 
              parent="menu_lead_website_root" 
              action="action_crm_lead_website_showcase"
              sequence="10"/>
    
</odoo>

----------------------------------------
FILE: ./views/email_templates.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <record id="email_lead_approval_request" model="mail.template">
        <field name="name">Lead Approval Request</field>
        <field name="email_from">{{ (object.company_id.email or object.env.user.company_id.email) }}</field>
        <field name="subject">New Lead Needs Approval: {{ object.name }}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html" type="html">
            <div style="font-family: Arial; font-size: 12px;">
                <p>Hello,</p>
                
                <p>A new lead has been submitted and requires your review:</p>
                
                <table style="border: 1px solid #ccc; padding: 10px;">
                    <tr><td><strong>Lead Name:</strong></td><td><t t-out="object.name"/></td></tr>
                    <tr><td><strong>Category:</strong></td><td><t t-out="object.lead_category.name or 'N/A'"/></td></tr>
                    <tr><td><strong>Country:</strong></td><td><t t-out="object.country_region or 'N/A'"/></td></tr>
                    <tr><td><strong>Description:</strong></td><td><t t-out="object.short_description or 'N/A'"/></td></tr>
                    <tr><td><strong>Submitted by:</strong></td><td><t t-out="object.create_uid.name"/></td></tr>
                    <tr><td><strong>Date:</strong></td><td><t t-out="object.create_date.strftime('%d/%m/%Y %H:%M')"/></td></tr>
                </table>
                
                <p>
                    <a t-attf-href="/web#id={{ object.id }}&amp;model=crm.lead&amp;view_type=form" style="background-color: #007bff; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px;">
                        Review Lead
                    </a>
                </p>
                
                <p>Thank you,<br/>CRM System</p>
            </div>
        </field>
    </record>
    
    <record id="email_lead_approved" model="mail.template">
        <field name="name">Lead Approved</field>
        <field name="email_from">{{ (object.company_id.email or object.env.user.company_id.email) }}</field>
        <field name="subject">Your Lead Has Been Approved: {{ object.name }}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html" type="html">
            <div style="font-family: Arial; font-size: 12px;">
                <p>Dear <t t-out="object.create_uid.name"/>,</p>
                
                <p>Great news! Your lead "<strong><t t-out="object.name"/></strong>" has been approved and is now visible on our website.</p>
                
                <p>
                    <a t-attf-href="/leads/{{ object.id }}">
                        View on Website
                    </a>
                </p>
                
                <p>Best regards,<br/>CRM Team</p>
            </div>
        </field>
    </record>
    
    <record id="email_lead_rejected" model="mail.template">
        <field name="name">Lead Rejected</field>
        <field name="email_from">{{ (object.company_id.email or object.env.user.company_id.email) }}</field>
        <field name="subject">Your Lead Needs Revision: {{ object.name }}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html" type="html">
            <div style="font-family: Arial; font-size: 12px;">
                <p>Hello <t t-out="object.create_uid.name"/>,</p>
                
                <p>Your lead "<strong><t t-out="object.name"/></strong>" has been reviewed and requires some revisions:</p>
                
                <p><strong>Reason:</strong></p>
                <p><t t-out="object.rejection_reason or 'Please see the details for more information'"/></p>
                
                <p>Please update your lead and resubmit it for approval.</p>
                
                <p>Thank you!</p>
            </div>
        </field>
    </record>
    
    <record id="email_lead_inquiry" model="mail.template">
        <field name="name">Lead Inquiry Received</field>
        <field name="email_from">{{ (object.company_id.email or object.env.user.company_id.email) }}</field>
        <field name="subject">New Inquiry for Lead: {{ object.name }}</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="body_html" type="html">
            <div style="font-family: Arial; font-size: 12px;">
                <p>Hello,</p>
                
                <p>You have received a new inquiry on a lead!</p>
                
                <table style="border: 1px solid #ccc; padding: 10px;">
                    <tr><td><strong>Lead Opportunity:</strong></td><td><t t-out="object.name"/></td></tr>
                    <tr><td><strong>From:</strong></td><td><t t-out="object.email_from"/></td></tr>
                    <tr><td><strong>Phone:</strong></td><td><t t-out="object.phone"/></td></tr>
                    <tr><td><strong>Message:</strong></td><td><t t-out="object.description"/></td></tr>
                </table>
                
                <p>
                    <a t-attf-href="/web#id={{ object.id }}&amp;model=crm.lead&amp;view_type=form" style="background-color: #007bff; color: white; padding: 10px 15px; text-decoration: none; border-radius: 5px;">
                        View Opportunity
                    </a>
                </p>
                
                <p>Thank you,<br/>Website CRM</p>
            </div>
        </field>
    </record>

</odoo>

----------------------------------------
FILE: ./views/lead_reject_wizard_views.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_lead_reject_wizard_form" model="ir.ui.view">
        <field name="name">lead.reject.wizard.form</field>
        <field name="model">lead.reject.wizard</field>
        <field name="arch" type="xml">
            <form string="Từ chối Lead">
                <group>
                    <field name="lead_id" invisible="1"/>
                    <field name="rejection_reason" placeholder="Vui lòng nhập lý do từ chối..."/>
                </group>
                <footer>
                    <button string="Xác nhận Từ chối" name="action_confirm_reject" type="object" class="btn-danger"/>
                    <button string="Hủy bỏ" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>

----------------------------------------
FILE: ./views/website_lead_gallery_template.xml
----------------------------------------

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="website_lead_gallery_template_inherit" inherit_id="website_lead_showcase.lead_detail">
        <xpath expr="//div[hasclass('lead-full-description')]/.." position="after">
            <t t-if="lead.gallery_image_ids">
                <div class="mt-5 mb-5 pt-4 border-top">
                    <h3 class="mb-4"><i class="fa fa-picture-o text-primary"></i> Hình ảnh &amp; Thông tin chi tiết</h3>
                    <div class="row g-5">
                        <t t-foreach="lead.gallery_image_ids" t-as="img">
                            <div class="col-12">
                                <div class="card shadow-sm border-0 h-100 bg-light rounded-3 overflow-hidden">
                                    <div class="p-3 text-center" style="background: white;">
                                        <img t-attf-src="/web/image/lead.gallery.image/#{img.id}/image" 
                                             class="img-fluid rounded" 
                                             style="max-height: 500px; width: auto; object-fit: contain;" 
                                             alt="Ảnh sản phẩm"/>
                                    </div>
                                    <div class="card-footer bg-light border-0 p-4">
                                        <t t-if="img.name and img.name != 'Ảnh sản phẩm'">
                                            <h5 class="mb-0 text-dark" style="line-height: 1.5;"><t t-esc="img.name"/></h5>
                                        </t>
                                        <t t-else="">
                                            <p class="mb-0 text-muted font-italic"><t t-esc="img.name"/></p>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
            </t>
        </xpath>
    </template>
</odoo>
----------------------------------------
FILE: ./views/website_templates.xml
----------------------------------------

<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Leads List Template -->
    <template id="leads_list" name="Leads List">
        <t t-call="website.layout">
            <div class="container my-5">
                
                <!-- Header -->
                <div class="row mb-5">
                    <div class="col-lg-8">
                        <h1 class="display-4">Business Opportunities</h1>
                        <p class="lead">Discover the latest leads and opportunities</p>
                    </div>
                </div>
                
                <!-- Filters -->
                <div class="row mb-4">
                    <div class="col-lg-12">
                        <form method="get" class="row g-3">
                            
                            <!-- Search -->
                            <div class="col-md-4">
                                <input type="text" 
                                       name="search" 
                                       class="form-control" 
                                       placeholder="Search leads..."
                                       t-att-value="search_query"/>
                            </div>
                            
                            <!-- Category Filter -->
                            
                            <div class="col-md-2">
                                <select name="intent" class="form-select">
                                    <option value="">Tất cả nhu cầu</option>
                                    <option value="buy" t-att-selected="selected_intent == 'buy'">TÌM MUA</option>
                                    <option value="sell" t-att-selected="selected_intent == 'sell'">CHÀO BÁN</option>
                                </select>
                            </div>

                                <div class="col-md-3">
                                <select name="category_id" class="form-select">
                                    <option value="">All Categories</option>
                                    <t t-foreach="categories" t-as="cat">
                                        <option t-att-value="cat.id"
                                                t-att-selected="str(cat.id) == str(selected_category)">
                                            <t t-esc="cat.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            
                            <!-- Country Filter -->
                            <div class="col-md-3">
                                <select name="country" class="form-select">
                                    <option value="">All Countries</option>
                                    <t t-foreach="countries" t-as="country_opt">
                                        <option t-att-value="country_opt"
                                                t-att-selected="country_opt == selected_country">
                                            <t t-esc="country_opt"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            
                            <!-- Search Button -->
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-primary w-100">
                                    Search
                                </button>
                            </div>
                            
                        </form>
                    </div>
                </div>
                
                <!-- Leads Grid -->
                <div class="row">
                    <t t-if="leads">
                        <t t-foreach="leads" t-as="lead">
                            <div class="col-lg-4 col-md-6 mb-4">
                                <div class="card h-100 lead-card shadow-sm">
                                    
                                    <!-- Lead Image -->
                                    <div class="lead-image-container">
                                        <t t-if="lead.lead_image">
                                            <img t-attf-src="/web/image/crm.lead/{{ lead.id }}/lead_image" 
                                                 class="card-img-top" 
                                                 alt="Lead"/>
                                        </t>
                                        <t t-else="">
                                            <div class="card-img-top bg-light d-flex align-items-center justify-content-center"
                                                 style="height: 250px;">
                                                <i class="fa fa-image fa-3x text-muted"></i>
                                            </div>
                                        </t>
                                    </div>
                                    
                                    <!-- Card Body -->
                                    <div class="card-body">
                                        
                                        <!-- Category Badge -->
                                        
                                        <t t-if="lead.lead_intent">
                                            <span t-attf-class="badge mb-2 {{ 'bg-danger' if lead.lead_intent == 'buy' else 'bg-success' if lead.lead_intent == 'sell' else 'bg-info' }}">
                                                <t t-esc="dict(lead.fields_get(['lead_intent'])['lead_intent']['selection'])[lead.lead_intent]"/>
                                            </span>
                                        </t>
    
                                        <t t-if="lead.lead_category">
                                            <span class="badge bg-secondary mb-2">
                                                <t t-esc="lead.lead_category.name"/>
                                            </span>
                                        </t>
                                        
                                        <!-- Lead Title -->
                                        <h5 class="card-title">
                                            <t t-esc="lead.name"/>
                                        </h5>
                                        
                                        <!-- Country/Region -->
                                        <p class="text-muted small">
                                            <i class="fa fa-map-marker"></i>
                                            <t t-esc="lead.country_region"/>
                                        </p>
                                        
                                        <!-- Description -->
                                        <p class="card-text text-truncate" 
                                           t-att-title="lead.short_description">
                                            <t t-esc="lead.short_description"/>
                                        </p>
                                        
                                        <!-- Published Date -->
                                        <p class="text-muted small">
                                            <i class="fa fa-calendar"></i>
                                            Posted: 
                                            <t t-if="lead.website_published_date">
                                                <t t-esc="lead.website_published_date.strftime('%d/%m/%Y')"/>
                                            </t>
                                        </p>
                                        
                                        <!-- View Count -->
                                        <p class="text-muted small">
                                            <i class="fa fa-eye"></i>
                                            <t t-esc="lead.website_view_count"/> views
                                        </p>
                                        
                                    </div>
                                    
                                    <!-- Card Footer -->
                                    <div class="card-footer bg-white">
                                        <a t-attf-href="/leads/{{ lead.id }}" 
                                           class="btn btn-primary btn-sm w-100">
                                            View Details →
                                        </a>
                                    </div>
                                    
                                </div>
                            </div>
                        </t>
                    </t>
                    <t t-else="">
                        <div class="col-12">
                            <div class="alert alert-info text-center">
                                <h4>No leads found</h4>
                                <p>Try adjusting your search filters</p>
                            </div>
                        </div>
                    </t>
                </div>
                
                <!-- Pagination -->
                <t t-if="total_pages &gt; 1">
                    <nav class="mt-5">
                        <ul class="pagination justify-content-center">
                            
                            <!-- Previous -->
                            <li t-attf-class="page-item {{ 'disabled' if page == 1 else '' }}">
                                <a class="page-link" 
                                   t-attf-href="/leads?page={{ page - 1 }}">
                                    Previous
                                </a>
                            </li>
                            
                            <!-- Page numbers -->
                            <t t-foreach="range(1, total_pages + 1)" t-as="pg">
                                <li t-attf-class="page-item {{ 'active' if pg == page else '' }}">
                                    <a class="page-link" t-attf-href="/leads?page={{ pg }}">
                                        <t t-esc="pg"/>
                                    </a>
                                </li>
                            </t>
                            
                            <!-- Next -->
                            <li t-attf-class="page-item {{ 'disabled' if page == total_pages else '' }}">
                                <a class="page-link" 
                                   t-attf-href="/leads?page={{ page + 1 }}">
                                    Next
                                </a>
                            </li>
                            
                        </ul>
                    </nav>
                </t>
                
            </div>
        </t>
    </template>
    
    <!-- Lead Detail Template -->
    <template id="lead_detail" name="Lead Detail">
        <t t-call="website.layout">
            <div class="container my-5">
                
                <!-- Back Link -->
                <div class="mb-4">
                    <a href="/leads" class="btn btn-outline-secondary">
                        ← Back to Leads
                    </a>
                </div>
                
                <div class="row">
                    
                    <!-- Main Content -->
                    <div class="col-lg-8">
                        
                        <!-- Lead Image -->
                        <div class="mb-4">
                            <t t-if="lead.lead_image">
                                <img t-attf-src="/web/image/crm.lead/{{ lead.id }}/lead_image" 
                                     class="img-fluid rounded" 
                                     alt="Lead"/>
                            </t>
                        </div>
                        
                        <!-- Lead Info -->
                        <h1 class="mb-3"><t t-esc="lead.name"/></h1>
                        
                        <!-- Business Profile Info -->
                        <div class="card bg-white shadow-sm border border-light mb-4 rounded-3">
                            <div class="card-body">
                                <h5 class="card-title text-primary mb-3 border-bottom pb-2">
                                    <i class="fa fa-info-circle"></i> Thông tin doanh nghiệp
                                </h5>
                                <div class="row g-3">
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded-circle p-2 me-3 text-center" style="width: 40px; height: 40px;">
                                                <i class="fa fa-building text-secondary"></i>
                                            </div>
                                            <div>
                                                <small class="text-muted d-block" style="font-size: 0.8rem;">Tên tổ chức</small>
                                                <strong><t t-esc="lead.partner_name or lead.contact_name or 'Đang cập nhật'"/></strong>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded-circle p-2 me-3 text-center" style="width: 40px; height: 40px;">
                                                <i class="fa fa-map-marker text-danger"></i>
                                            </div>
                                            <div>
                                                <small class="text-muted d-block" style="font-size: 0.8rem;">Quốc gia / Khu vực</small>
                                                <strong><t t-esc="lead.country_region or lead.city or 'Đang cập nhật'"/></strong>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded-circle p-2 me-3 text-center" style="width: 40px; height: 40px;">
                                                <i class="fa fa-calendar text-info"></i>
                                            </div>
                                            <div>
                                                <small class="text-muted d-block" style="font-size: 0.8rem;">Ngày đăng tải</small>
                                                <strong>
                                                    <t t-if="lead.website_published_date">
                                                        <t t-esc="lead.website_published_date.strftime('%d/%m/%Y')"/>
                                                    </t>
                                                    <t t-else="">N/A</t>
                                                </strong>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded-circle p-2 me-3 text-center" style="width: 40px; height: 40px;">
                                                <i class="fa fa-bullseye text-success"></i>
                                            </div>
                                            <div>
                                                <small class="text-muted d-block" style="font-size: 0.8rem;">Nhu cầu</small>
                                                <t t-if="lead.lead_intent">
                                                    <span t-attf-class="badge {{ 'bg-danger' if lead.lead_intent == 'buy' else 'bg-success' if lead.lead_intent == 'sell' else 'bg-info' }}">
                                                        <t t-esc="dict(lead.fields_get(['lead_intent'])['lead_intent']['selection'])[lead.lead_intent]"/>
                                                    </span>
                                                </t>
                                                <t t-if="lead.lead_category">
                                                    <span class="badge bg-secondary ms-1">
                                                        <t t-esc="lead.lead_category.name"/>
                                                    </span>
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Full Description -->
                        <div class="mb-5">
                            <h3>Description</h3>
                            <div class="lead-full-description">
                                <t t-esc="lead.description or lead.short_description"/>
                            </div>
                        </div>
                        
                        <!-- Related Leads -->
                        <t t-if="related_leads">
                            <div class="mb-5">
                                <h3>Related Opportunities</h3>
                                <div class="row">
                                    <t t-foreach="related_leads" t-as="related">
                                        <div class="col-md-6 mb-3">
                                            <div class="card h-100">
                                                <t t-if="related.lead_image">
                                                    <img t-attf-src="/web/image/crm.lead/{{ related.id }}/lead_image"
                                                         class="card-img-top"
                                                         alt="Related Lead"
                                                         style="height: 200px; object-fit: cover;"/>
                                                </t>
                                                <div class="card-body">
                                                    <h6 class="card-title">
                                                        <t t-esc="related.name"/>
                                                    </h6>
                                                    <p class="card-text small">
                                                        <t t-esc="str(related.short_description)[:100]"/>...
                                                    </p>
                                                </div>
                                                <div class="card-footer bg-white">
                                                    <a t-attf-href="/leads/{{ related.id }}"
                                                       class="btn btn-sm btn-primary">
                                                        View
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </t>
                        
                    </div>
                    
                    <!-- Sidebar -->
                    <div class="col-lg-4">
                        
                        <!-- Contact Form -->
                        <div class="card shadow-sm">
                            <div class="card-header bg-primary text-white">
                                <h5 class="mb-0"><i class="fa fa-handshake-o"></i> Yêu cầu Kết nối / Báo giá</h5>
                            </div>
                            <div class="card-body">
                                <form id="lead_contact_form" onsubmit="submitLeadForm(event)">
                                    
                                    <!-- Name -->
                                    <div class="mb-3">
                                        <label class="form-label">Tên Doanh nghiệp / Cá nhân *</label>
                                        <input type="text" 
                                               name="contact_name" 
                                               class="form-control" 
                                               required="1"/>
                                    </div>
                                    
                                    <!-- Email -->
                                    <div class="mb-3">
                                        <label class="form-label">Email liên hệ *</label>
                                        <input type="email" 
                                               name="contact_email" 
                                               class="form-control" 
                                               required="1"/>
                                    </div>
                                    
                                    <!-- Phone -->
                                    <div class="mb-3">
                                        <label class="form-label">Số điện thoại</label>
                                        <input type="tel" 
                                               name="contact_phone" 
                                               class="form-control"/>
                                    </div>
                                    
                                    <!-- Message -->
                                    <div class="mb-3">
                                        <label class="form-label">Giới thiệu năng lực &amp; Lời nhắn *</label>
                                        <textarea name="message" 
                                                  class="form-control" 
                                                  rows="5" 
                                                  required="1"></textarea>
                                    </div>
                                    
                                    <!-- Submit -->
                                    <button type="submit" class="btn btn-success w-100">
                                        Gửi Yêu Cầu Kết Nối
                                    </button>
                                    
                                </form>
                                
                                <!-- Form Status Messages -->
                                <div id="form_status_message" class="mt-3"></div>

                                <script type="text/javascript">
                                    async function submitLeadForm(e) {
                                        e.preventDefault();
                                        const form = e.target;
                                        const parts = window.location.pathname.split('/');
                                        const leadId = parts[parts.length - 1];
                                        
                                        const submitBtn = form.querySelector('button[type="submit"]');
                                        const originalText = submitBtn.textContent;
                                        submitBtn.disabled = true;
                                        submitBtn.textContent = 'Đang xử lý...';
                                        
                                        try {
                                            const formData = new FormData(form);
                                            const response = await fetch(`/leads/${leadId}/contact`, {
                                                method: 'POST',
                                                body: formData
                                            });
                                            
                                            const result = await response.json();
                                            const statusDiv = document.getElementById('form_status_message');
                                            
                                            if (result.status === 'success') {
                                                statusDiv.innerHTML = `<div class="alert alert-success mt-3">${result.message}</div>`;
                                                form.reset();
                                            } else {
                                                statusDiv.innerHTML = `<div class="alert alert-danger mt-3">${result.message}</div>`;
                                            }
                                        } catch (error) {
                                            document.getElementById('form_status_message').innerHTML = '<div class="alert alert-danger mt-3">Lỗi kết nối. Vui lòng tải lại trang!</div>';
                                        } finally {
                                            submitBtn.disabled = false;
                                            submitBtn.textContent = originalText;
                                        }
                                    }
                                </script>

                            </div>
                        </div>
                        
                        <!-- Block Bảo mật thông tin -->
                        <div class="card shadow-sm mt-4 border-warning">
                            <div class="card-header bg-warning text-dark">
                                <h5 class="mb-0"><i class="fa fa-lock"></i> Thông tin đối tác</h5>
                            </div>
                            <div class="card-body text-center">
                                <p class="text-muted mb-2">Thông tin liên hệ trực tiếp đã được bảo mật theo quy định của nền tảng.</p>
                                <p class="mb-0 fw-bold">Vui lòng điền form phía trên để chúng tôi hỗ trợ kết nối bạn!</p>
                            </div>
                        </div>
                        
                        <!-- Block Chia sẻ -->
                        <div class="card shadow-sm mt-4">
                            <div class="card-body text-center">
                                <p class="mb-2"><strong>Chia sẻ cơ hội này:</strong></p>
                                <button class="btn btn-outline-primary btn-sm mx-1" onclick="navigator.clipboard.writeText(window.location.href); alert('Đã copy link thành công!');">
                                    <i class="fa fa-link"></i> Copy Link
                                </button>
                            </div>
                        </div>

                        <!-- Stats -->
                        <div class="card shadow-sm mt-4">
                            <div class="card-body text-center">
                                <p class="mb-0 text-muted">
                                    <i class="fa fa-eye"></i>
                                    <strong><t t-esc="lead.website_view_count"/></strong> views
                                </p>
                                <p class="mb-0 text-muted">
                                    <i class="fa fa-comment"></i>
                                    <strong><t t-esc="lead.website_contact_count"/></strong> inquiries
                                </p>
                            </div>
                        </div>
                        
                    </div>
                    
                </div>
                
            </div>
        </t>
    </template>
    
</odoo>

----------------------------------------
FILE: ./wizard/__init__.py
----------------------------------------

from . import lead_reject_wizard

----------------------------------------
FILE: ./wizard/lead_reject_wizard.py
----------------------------------------

from odoo import models, fields

class LeadRejectWizard(models.TransientModel):
    _name = 'lead.reject.wizard'
    _description = 'Wizard Nhập lý do từ chối Lead'

    lead_id = fields.Many2one('crm.lead', string="Lead", required=True)
    rejection_reason = fields.Text(string="Lý do từ chối", required=True)

    def action_confirm_reject(self):
        self.lead_id.write({
            'approval_status': 'rejected',
            'website_visible': False,
            'rejection_reason': self.rejection_reason
        })
        self.env['lead.approval.history'].create({
            'lead_id': self.lead_id.id,
            'action': 'rejected',
            'performed_by': self.env.user.id,
            'notes': f'Lead bị từ chối: {self.rejection_reason}',
        })
        # Gửi email từ chối
        template = self.env.ref('website_lead_showcase.email_lead_rejected', raise_if_not_found=False)
        if template:
            template.send_mail(self.lead_id.id, force_send=True)
